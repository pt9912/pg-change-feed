# Bau-Ausgaben eines lokalen `dotnet build`/`dotnet test` (Docker-only ist die
# Vorgabe, AGENTS.md §3.1 — dieser Eintrag schützt nur gegen einen versehentlich
# lokal gestarteten Host-Bau).
bin/
obj/
