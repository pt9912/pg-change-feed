# Harness

---

## Purpose

Dieser Harness verbindet bestehende Spezifikationen, ADRs,
Planning-Dokumente und Gates. Er ist **kein Ersatz** für `spec/` oder
`docs/`, sondern ein **Einstiegspunkt** für Menschen und AI-Code-Agenten.

Wenn diese Datei einer kanonischen Quelle widerspricht, **gewinnt die
kanonische Quelle**, und diese Datei wird angepasst.

Strukturregeln (Verzeichniskonvention, ID-Schemata, Modus-Deklarationen
pro Sub-Area, Zusatzklassen für Sensors-Bindung) sowie Adaptionen ggü.
der adoptierten Baseline leben in [`conventions.md`](conventions.md).
Diese Datei dupliziert sie nicht.

## Source precedence

| Rang | Datei | Charakter |
|---|---|---|
| 1 | [`spec/lastenheft.md`](../spec/lastenheft.md) | vertraglich abnahmebindend |
| 2 | [`spec/pflichtenheft.md`](../spec/pflichtenheft.md) | technisch fortschreibbar |
| 3 | [`spec/architecture.md`](../spec/architecture.md) | Komponenten/Sequenzen, meilensteinfrei |
| 4 | [`docs/plan/adr/`](../docs/plan/adr/) | Architekturentscheidungen |
| 5 | [`docs/plan/planning/in-progress/roadmap.md`](../docs/plan/planning/in-progress/roadmap.md) | Wellen-Sequenz |
| 6 | [`docs/user/`](../docs/user/) | Operations, Quality, Releasing |
| 7 | [`README.md`](../README.md) | Projekt-Überblick |
| 8 | [`AGENTS.md`](../AGENTS.md) | Agent-Briefing |
| 9 | diese Datei | Harness-Einstieg |

> Die Ränge 1–3 sind die **drei Spec-Straten** — Vertrag, Technik, Sicht —,
> und alle drei sind obligatorisch (Baseline-Regelwerk
> `grundlagen-referenz-richtung.md` §Spec-Straten). **Adaption ist die
> Zwei-Straten-Form, nicht die Drei-Straten-Form**: Wer Rang 2 streicht,
> deklariert das als `MR-<NNN>` in [`conventions.md`](conventions.md) und
> nummeriert neu (dann acht Ränge).

## Guides (Feedforward-Quellen)

<!--
Was lenkt den Agenten *vor* der Handlung? Pointer, kein Inhalt.
-->

| Quelle | Inhalt |
|---|---|
| [`spec/lastenheft.md`](../spec/lastenheft.md) | Anforderungen, IDs, Akzeptanzkriterien |
| [`spec/pflichtenheft.md`](../spec/pflichtenheft.md) | technische Details, Defaults |
| [`spec/architecture.md`](../spec/architecture.md) | Komponenten, Schichten, Constraints |
| [`docs/plan/adr/`](../docs/plan/adr/) | Architekturentscheidungen |
| [`docs/plan/planning/`](../docs/plan/planning/) | Slice-Pläne und Roadmap |
| [`AGENTS.md`](../AGENTS.md) | Hard Rules, Source Precedence, Workflow |
| [`conventions.md`](conventions.md) | repo-lokale Strukturregeln, Adaptions-Block (`MR-*`), Modus-Deklarationen |
| `.harness/skills/reviewer.md` | Reviewer-Skill: HIGH-Liste, Kategorien-Regeln, Negativbefund-Pflicht, Output-Schema (Modul 10) — nächste Rolle nach Schritt 8 des Minimal Agent Workflow, nicht Teil der Implementer-Eingabe |
| `.harness/baseline/<tag>/regelwerk/` (vendored; `README.md` = Index) | adoptiertes Betriebsregelwerk in Agenten-Kurzform — **präsente nachschlagbare Vertiefung**, pro Entscheidung abschnittsweise (siehe [`AGENTS.md`](../AGENTS.md) §1); derivativ, Stand/Tag siehe [`conventions.md`](conventions.md) §Baseline |
| `.harness/baseline/<tag>/templates/` (vendored, parallel) | Referenz-Form der Skelette, auf die das Regelwerk mit `../templates/…` als „Ziel-Form" verweist (netzlos, weil parallel zu `regelwerk/`); Vorlagen zum Kopieren-und-Ausfüllen |

## Sensors (Feedback-Gates)

<!--
WICHTIG: Nur Befehle aufzählen, die im Makefile *existieren*.
Halluzinierte Gates sind die häufigste Form von Harness-Lüge (Modul 13).

Drei Spalten — kein Lauf-Status:
- Target:  der Make-Befehl.
- Vertrag: was prüft das Gate (was wäre verletzt, wenn es rot wird).
- Bindung: strukturelle Referenzen — Carveout-ID (`CO-<NNN>`),
  Slice-ID, Schwelle, Image-Hash, ADR-ID. NICHT der Lauf-Status,
  sondern was das Gate *strukturell trägt*.

Lauf-Wahrheit pro Commit liegt in CI (Badge/Dashboard), nicht hier
(`harness/README.md` ist Rang 9 in der Source Precedence).
Strukturell rote Gates (dauerhaft rot) bekommen einen Carveout in
`docs/plan/carveouts/CO-<NNN>-…` mit Auflösungs-Trigger und Folge-Slice
(Modul 7); die Bindung-Spalte verweist auf die `CO-<NNN>`-ID, die
Begründung lebt im Carveout, nicht hier.

Bei d-check-Einsatz (≥ v0.73.0) deckt Modul `reviews` (Ziel `doc-reviews`,
Review-Report-Deckung für `done/`-Slices mit Review-DoD-Haken) die
Code→Review-Kante ab, Modul `planning` (Ziel `doc-planning`,
Planning-Lifecycle-Konsistenz) die Verify→Closure-Kante — beide aus dem
Lebenszyklus-Diagramm in Modul 1. Nur eintragen, wenn das Ziel im
Makefile existiert (siehe oben).

NICHT-GATES: Ein Target, das der Agent braucht, aber das nichts über den
Zustand des Repos urteilt — es *bewegt* (Slice-Move), *misst* (Latenz) oder
*sagt*, was ein schreibender Lauf täte — steht in der zweiten Tabelle und
trägt `kein Gate` IN DER ZEILE SELBST, in der Spalte, die hier die Bindung
führt — nicht in Prosa daneben. Weglassen ist nur für Targets richtig, die
niemand braucht (Modul 13 §Vorhanden ≠ behauptet).

WÄCHST DIE SEKTION: Die Tabelle bleibt klein, die Prosa darunter nicht.
Braucht ein Gate mehr als EINEN SATZ — Deckungsgrenze, Ausgabe-Bedeutung,
Exit-Codes, Abbruch-Bedingungen —, wandert das nach. Ob der Überhang schon
unter der Tabelle steht oder in die Zelle gedrängt wurde, ist dieselbe Sache:
eine Zelle, die zum Absatz geworden ist, ist der Fund, nicht die Ausnahme.
`harness/sensors/<target>.md`, und die **Target-Zelle wird zum Link darauf**
— wie die `MR`-Zelle im Adaptions-Block. Der Link ist kein Komfort: Er ist die
einzige Fassung dieser Zuordnung, die der Link-Sensor prüft. Eine bloße
Namenskonvention (`make X` -> `sensors/X.md`) bleibt still grün, wenn die
Datei verschwindet und die Zeile stehen bleibt. Seine Grenze: Er prüft EINE
Richtung — ob das Ziel existiert; eine Datei ohne Index-Zeile und eine Zeile
auf die falsche Datei bleiben still grün, und geprüft wird nur, wo ein
Link-Sensor über `harness/` läuft. Kein `sensors/done/`:
ein retiriertes Gate verschwindet, `git` hält seine Geschichte. Was das
Werkzeug selbst deckt (welcher Test welche Hälfte trägt), gehört NICHT
dorthin, sondern in seine ADR/Spec-Zeile/seinen Skriptkopf.
-->

| Target | Vertrag | Bindung |
|---|---|---|
| `make baseline-verify` | verifiziert die vendored Baseline netzlos (Integrität + Vollständigkeit gegen `SHA256SUMS`) | [`harness/sensors/baseline-verify.md`](sensors/baseline-verify.md) |
| `make docs-check` | kaputte Referenzen in der Markdown-Doku (links, anchors, ids, matrix, versions, structure, hostpaths, tracked) | [`harness/sensors/docs-check.md`](sensors/docs-check.md) |
| `make a-check` | prüft die Hexagon-Schichten-Edges aus `.a-check.yml` gegen den Go-Baum — netzlos, read-only, digest-gepinntes Release-Image | [`ADR-0041`](../docs/plan/adr/0041-a-check-maschinenform-architekturpruefung.md) · [`harness/sensors/a-check.md`](sensors/a-check.md) |
| `make commit-traceability` | Commit-Message-Traceability: je Message der Range ≥ 1 `LH-*`-/`ADR-*`-Kennung (d-check Modul `commits`, Befund `commit-untraceable`) und keine `SPEC-*`/`ARC-*`-Kennung im Betreff (`tools/harness/commit-traceability.sh`); Standing-Gate über die letzten 5 Commits (`RANGE=base..head` überschreibt) | [`ADR-0045`](../docs/plan/adr/0045-commit-traceability-standing-gate.md) · seit slice-006 |
| `make coverage-gate` | Go-Test-Coverage über die netzlos prüfbare Fläche (`./internal/...`+`./cmd/...`+`./gen/...` ohne die Pakete, deren Testlauf einen externen Dienst voraussetzt) gegen `THRESHOLD` — bootstrap-aware Gate, Rampe Einstieg 70 % → Endstufe 80 %; geltende Stufe in [`harness/mk/coverage.mk`](mk/coverage.mk); vor dem Bau hält `tools/harness/db-package-lists-check.sh` die namentlichen Paketlisten des ausgenommenen Gegenstands gleich (Exit ≠ 0 färbt das Ziel rot) | [`ADR-0071`](../docs/plan/adr/0071-coverage-gate-messgegenstand-netzlos-pruefbare-flaeche.md) · [`ADR-0054`](../docs/plan/adr/0054-coverage-gate-und-benchmark-infrastruktur.md) · [`ADR-0077`](../docs/plan/adr/0077-coverage-rampen-neu-bemessung-subjekt-transfer.md) · [`harness/sensors/coverage-gate.md`](sensors/coverage-gate.md) · seit slice-049 |
| `make generated-sync` | prüft, dass der committete Protobuf-/gRPC-Code byte-gleich der Ausgabe des **gepinnten** Generators (Dockerfile-Stufe `proto-export`, dieselbe Build-Zeit-Erzeugung wie `make proto-generate`, seit `slice-generated-sync-tar-export`) aus den committeten `.proto`-Quellen ist; die host-seitige `tar`-Extraktion (kein Bind-Mount, kein `--user`-Workaround) landet in einem Temp-Verzeichnis, der Arbeitsbaum bleibt unberührt | [`harness/sensors/generated-sync.md`](sensors/generated-sync.md) · seit slice-090 |
| `make gates` | alle inneren Gates (baseline-verify, docs-check, a-check, commit-traceability, coverage-gate, generated-sync), Nachweis-Stempel zuletzt | — |
| `<make-target>` | volle Closure | Image-Hash `sha256:…` (Modul 14) |

**Werkzeuge — genannt, weil der Lauf sie braucht, aber kein Gate:**

| Target | Tut was | Bindung |
|---|---|---|
| `make image` | baut das OCI-Image (Multi-Stage, digest-gepinnt); **Image-Digest** nach `harness/image-hash.txt` — der Digest ist der **Lauf-Beleg** des letzten offiziellen `make image`-Laufs (builder- und lauf-gebunden, kein Inhalts-Fingerabdruck); ein Digest-Vergleich über Umgebungen/Läufe ist kein Staleness-Beweis, Inhalts-Streits entscheidet der sha256 des extrahierten Binaries (Container-Export). Die Datei ist **lokal, nicht committet** — der lokale `:dev`-Pfad (ohne `VERSION`) wird nie gepusht, also braucht keine über Commits hinweg nachschlagbare Historie zu existieren ([`ADR-0103`](../docs/plan/adr/0103-image-hash-lokal-statt-committet.md)); ein Zug, der Build-Kontext-Dateien ändert, läuft `make image` weiterhin vor seiner Closure, ohne Commit-Schritt. Mit `VERSION=<semver>` (optional `LATEST=true`, [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Entscheidung 1/2) pusht **derselbe** Build-Aufruf stattdessen nach GHCR **und** Docker Hub (Content-Mirror, kein zweiter Build), jetzt als Multi-Arch-Manifestliste über `--platform linux/amd64,linux/arm64` (ein Buildx-Aufruf, kein zweiter Build je Plattform; die bestehende Digest-Extraktion liefert dabei unverändert den Index-Digest, real gegen eine lokale Test-Registry geprüft) — dieser Pfad braucht Registry-Login und läuft real nur innerhalb von `release.yml`; der Digest dieses Laufs bekommt dort einen eigenen, dauerhaften Beleg (GitHub-Release-Beschreibungstext), kein Widerspruch zu `ADR-0103`s Begründung für den unveränderten, bewusst einplattformigen `:dev`-Pfad (`--load` bleibt für eine konsistente, schnelle lokale Dev-Iteration einplattformig — ob `--load` überhaupt eine Multi-Platform-Manifestliste laden kann, hängt vom Storage-Treiber ab, real bestätigt: mit aktiviertem containerd-Image-Store gelingt es, keine grundsätzliche buildx-Grenze) | kein Gate, [`ADR-0044`](../docs/plan/adr/0044-image-beleg-semantik.md), [`ADR-0103`](../docs/plan/adr/0103-image-hash-lokal-statt-committet.md), [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md), [`ADR-0042`](../docs/plan/adr/README.md) |
| `make image-stale` | meldet Base-Image-Drift: FROM-Digests des Dockerfile gegen die aktuellen Registry-Digests derselben Tags, plus Existenz des nächsten Major-Tags; braucht Netz | kein Gate, [`ADR-0039`](../docs/plan/adr/README.md) (Update = bewusster Digest-Commit, Modul 14) |
| `make image-cve` | Trivy CRITICAL/HIGH gegen das publizierte GHCR-`:latest`-Image (digest-gepinntes `aquasec/trivy`); löst die bereits seit `slice-001` angekündigte, bis hierhin unimplementierte Zusage ein; scheitert bis zum ersten echten Release strukturell am fehlenden Ziel-Image (real geprüft — kein `ghcr.io/pt9912/pg-change-feed:latest` vorhanden), kein Implementierungsfehler; automatisiert über `.github/workflows/image-scan.yml` (`schedule` nächtlich + `workflow_dispatch`, advisory, kein Gate, kein PR-Blocker) | kein Gate, [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Entscheidung 6 |
| `make pin-stale-race`/`make pin-stale-pgtest`/`make pin-stale-dmigrate`/`make pin-stale-acheck` | Upstream-Pin-Freshness P3–P6 ([`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Pin-Inventar): je Achse ein Digest-Vergleich einer Makefile-/`a-check.mk`-Variable (`TOOLCHAIN_RACE_IMAGE`, `PG_TEST_IMAGE`, `D_MIGRATE_IMAGE`, `A_CHECK_IMAGE`) gegen den aktuellen Registry-Digest, über das gemeinsame `tools/harness/pin-stale.sh` (analog `make image-stale`, aber Quelle ist eine Variable statt einer Dockerfile-`FROM`-Zeile); P5/P6 sind reine Digest-Pins ohne eigenen Tag, deshalb Vergleich gegen den jeweiligen `:latest`-Tag der GHCR-Pakete. Real ausgeführt: P3/P4/P5 zeigen echten Drift (Upstream-Tags wurden seit dem Pin neu gebaut), P6 ist aktuell | kein Gate, [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Entscheidung 7, braucht Netz |
| `make pin-stale-dcheck` | Upstream-Pin-Freshness P7 ([`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Pin-Inventar): zwei Achsen für `d-check.mk`s `DCHECK_IMAGE`/`DCHECK_DIGEST` — Digest-Drift (trägt der gepinnte Tag noch denselben Bau?) und Tag-Frische (existiert ein neuerer `pt9912/d-check`-Release als der gepinnte Tag, über die GitHub-Releases-API?) | kein Gate, [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Entscheidung 7, braucht Netz |
| `make pin-stale-baseline` | Upstream-Pin-Freshness P8 ([`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Pin-Inventar): die adoptierte Kurs-Baseline-Version (`harness/conventions.md` §Baseline) gegen den neuesten `pt9912/ai-harness-course`-Release (GitHub-Releases-API) — kein Docker-Pin, eine Baseline-Aktualisierung bleibt ein bewusster Bootstrap-Vorgang (Modul 2) | kein Gate, [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Entscheidung 7, braucht Netz |
| `make pin-stale-actions` | Upstream-Pin-Freshness P9 ([`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Pin-Inventar): jede SHA-gepinnte `uses:`-Zeile über alle `.github/workflows/*.yml` (`AGENTS.md` §3.8) gegen zwei Achsen — Tag-Mutation (`git ls-remote` gegen den im Kommentar genannten Tag; zeigt er noch auf denselben SHA?) und Tag-Frische (neuester Release des Action-Repos über die GitHub-Releases-API). Ein doppelt referenziertes Repo@Tag über mehrere Workflow-Dateien wird nur einmal geprüft | kein Gate, [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Entscheidung 7, braucht Netz |
| `make doc-tracked` | isolierter Einzel-Lauf des `tracked`-Moduls (Getrackt-Status auflösbarer, existierender Link-/Bild-Ziele gegen den git-Index, `--enable tracked` mit allen anderen Modulen `--disable`) — das Modul läuft bereits im `modules:`-Bündel mit und damit in `make docs-check`/`make gates`; dieses Ziel bleibt daneben als isoliertes Diagnose-Werkzeug bestehen, netzlos, braucht `.git` im Mount | kein Gate, [`harness/sensors/docs-check.md`](sensors/docs-check.md) §Bindung · seit slice-d-check-tracked-modul |
| `make doc-trace` | gibt die Requirements Traceability Matrix (RTM) auf stdout aus (`--trace`, **kein** eigenes Modul, advisory — Exit 0 auch mit Requirements-Waisen); `trace.requirements.id-pattern` in `.d-check.yml` verdrahtet die RTM-Quelle auf die `LH-(FA|QA)-[A-Z]{3}-\d{3}`-Kennungskonvention dieses Repos statt d-checks generischen Default, `trace.coverage` liest zusätzlich `docs/user/e2e-abdeckung.md`, `docs/user/bench-abdeckung.md`, `docs/user/ci-matrix-abdeckung.md` und `docs/user/sdk-e2e-abdeckung.md` (Label `SDK-E2E`, seit slice-sdk-csharp-reale2e) als kuratierte Coverage-Dimensionen, damit eine Anforderung mit ausschließlichem E2E-, Bench-, CI-Matrix- oder SDK-E2E-Nachweis nicht fälschlich als Waise erscheint (gemessen 2026-09-24 mit `make doc-trace` am Arbeitsbaum mit Lastenheft 0.13.0: 80 Anforderungen, **2 Waisen** — `LH-FA-CFG-007`/`LH-FA-CFG-008`, beide ohne Implementation; `LH-FA-CAP-009` trägt den Nachweis `E2E` über die Zeilen von `docs/user/e2e-abdeckung.md`; `LH-FA-SST-009` erscheint über die SDK-E2E-Dimension nicht mehr als Waise, sobald der Träger die Kennung trägt). `make doc-complete`/`--require-complete` bleibt trotzdem bewusst **nicht** in `GATE_CHECKS`/`make gates`: die Waisen-Zahl ist ein realer Momentaufnahme-Stand, kein struktureller Ausschluss künftiger Waisen — eine neue Anforderung ohne Beleg wäre erst beim nächsten `make doc-trace`-Lauf sichtbar, nicht automatisch gate-blockierend, solange `make doc-complete` nicht Teil der Closure ist | kein Gate, wie `make image-stale`, [`harness/sensors/docs-check.md`](sensors/docs-check.md) §Grenze · seit slice-d-check-trace-rtm |
| `make doc-ci-matrix` | fragt den jeweils letzten **erfolgreichen** Lauf von `.github/workflows/e2e.yml` (beide PostgreSQL-17/18-Matrix-Legs `success`, `LH-QA-POR-001`) und `ci.yml` (Schritt `Linux-Plattform-Assertion (LH-QA-POR-002)` `success`) über die öffentliche GitHub-REST-API ab — die Abfrage läuft Container-gekapselt (`TOOLCHAIN_IMAGE` wiederverwendet, `apk add curl jq` zur Laufzeit), kein `gh`/`curl` auf dem Host nötig (`AGENTS.md` §3.1). Schreibt bei Erfolg `docs/user/ci-matrix-abdeckung.md` (Lauf-ID/Commit/URL je Zeile als Beleg, `trace.coverage`-Dritteintrag in `.d-check.yml`, Label `CI-Matrix`); bricht ohne Datei-Schreibversuch mit `exit 1` ab, wenn kein erfolgreicher Lauf gefunden wird oder ein erwarteter Job/Schritt fehlt bzw. nicht `success` ist | kein Gate, braucht Netz (wie `make image-stale`), [`ADR-0105`](../docs/plan/adr/0105-ci-matrix-rtm-sichtbarkeit-por-001-002.md) |
| `make proto-generate` | erzeugt den Go-Code des Live-Change-Streams aus `proto/cdc/stream/v1/changestream.proto` — Docker-only: die Dockerfile-Stufe `proto` trägt `protoc` und die gepinnten Plugins `protoc-gen-go`/`protoc-gen-go-grpc`; die Folgestufe `proto-export` (seit slice-104) kopiert die `.proto`-Quelle per `COPY` hinein und ruft `protoc` **zur Build-Zeit** auf (kein Bind-Mount, kein `--user`-Workaround), ihr `ENTRYPOINT` gibt das Erzeugnis als `tar`-Stream über stdout aus; `tools/harness/proto-generate.sh` baut die Stufe und extrahiert **host-seitig** über `docker run --rm --network none <image> | tar -x -C .` — die Pipe läuft explizit unter `bash` mit `set -o pipefail`, damit ein `docker run`-Fehlschlag den Lauf sichtbar rot macht statt hinter `tar`s Exit-Code zu verschwinden (`AGENTS.md` §3.9; das Makefile-Rezept selbst liefe unter `/bin/sh`, dort `dash` ohne `pipefail`) — die extrahierten Dateien gehören dadurch automatisch dem aufrufenden Nutzer. Die erzeugten Dateien liegen committet im Baum und werden von `make test`/`make image` mitkompiliert; der Generator läuft nur, wenn sich die `.proto`-Quelle ändert | kein Gate, [`ADR-0060`](../docs/plan/adr/0060-grpc-streaming-mechanismus.md) |
| `make test` | Unit-Tests mit Race-Detector im gepinnten Toolchain-Container (netzlos; Modul-Cache im Docker-Volume, Vorbereitung: `make mod-download`); `TOOLCHAIN_RACE_IMAGE` ist Debian- statt Alpine-basiert, weil `go test -race` einen C-Compiler zum Linken braucht (`gcc`), den das Alpine-Image nicht trägt · seit slice-037 (`ADR-0050` Fitness Function: `mapper.Assembler` gegen gleichzeitigen Zugriff aus zwei Goroutinen) | kein Gate, [`ADR-0030`](../docs/plan/adr/0030-testpyramide.md) |
| `make test-store` | Adapter-Tests gegen reale PostgreSQL im Testcontainer (gepinnte Digests: Toolchain + PostgreSQL; Schema-Rollout über d-migrate vor dem Testlauf — dieselbe Kette wie der E2E-Lauf; DB-Daten im Container); trägt die Run-Store-Tests des Backfills (`postgresstorage`: Annahme von Run-Zeile `queued` und Antragsvermerk in einer Transaktion, Run-Zustand und Endzustände, ein atomarer Schreiber über alle Blöcke, Ordnung `0bf-…` vor WAL-Kennungen derselben Position unter der Kollation der Test-Datenbank, Rollen auf `cdc.backfill_run` und `cdc.administration_request` mit SQLSTATE `42501`, die drei Backfill-Adapter unter Login-Identitäten ihrer Rolle (`cdc_admin`/`cdc_capture`, ohne Superuser) und der ganze Antragsverarbeitungs-Pfad der Administrations-Goroutine (`internal/bootstrap`) unter denselben Logins; [`ADR-0111`](../docs/plan/adr/0111-backfill-bestand-snapshot-bulk-copy.md), [`ADR-0113`](../docs/plan/adr/0113-backfill-rollenschnitt-aufnahme-warnkriterium.md)); legt das `-coverprofile` des Store-Teils der DB-Adapter-Coverage ab | kein Gate, [`ADR-0030`](../docs/plan/adr/0030-testpyramide.md), [`ADR-0043`](../docs/plan/adr/0043-schemamigrationen-mit-d-migrate.md); DB-Adapter-Coverage ([`sensors/db-adapter-coverage.md`](sensors/db-adapter-coverage.md), [`ADR-0071`](../docs/plan/adr/0071-coverage-gate-messgegenstand-netzlos-pruefbare-flaeche.md)) |
| `make test-replication` | Replication-Stream-Tests gegen reale PostgreSQL mit Publication und Logical Replication Slot (`wal_level=logical`, gepinnte Digests: Toolchain + PostgreSQL; Schema-Stand des Tier-Laufs über denselben d-migrate-Rollout wie der Betrieb, `tools/schema/apply-rollout.sh`; DB-Daten im Container); legt das `-coverprofile` des Replication-Teils der DB-Adapter-Coverage ab (Paketliste: [`sensors/db-adapter-coverage.md`](sensors/db-adapter-coverage.md) §Gegenstand), mergt beide Profile zur Zahl und prüft sie gegen die Schwelle; `postgressnapshot` ([`ADR-0111`](../docs/plan/adr/0111-backfill-bestand-snapshot-bulk-copy.md) Teilfrage 1) belegt den temporären Slot mit Export-Snapshot, die Cursor-Blöcke, die Lesesperre und den Filenode-Vergleich bei einem Umschreiben zwischen Export und Import ([`ADR-0118`](../docs/plan/adr/0118-backfill-umschreiben-im-snapshot-fenster.md)) und die Bild-Parität von WAL- und Backfill-Pfad über den Typ-Satz ([`ADR-0115`](../docs/plan/adr/0115-backfill-spaltenwerte-text-ergebnisformat.md)), jeder seiner Tests überspringt ohne `CDC_REPLICATION_TEST_DSN`; die Phase `tier` fährt zusätzlich den Slot-Reserve-Test gegen einen eigenen PostgreSQL mit `max_replication_slots=1`; die Leerlauf-Bestätigung des Capture-Slots ([`ADR-0120`](../docs/plan/adr/0120-capture-slot-leerlauf-bestaetigung.md)) belegen im Paket `receive` die Form X1 (200.000 Zeilen in eine nicht veröffentlichte Tabelle: der Rückstand fällt unter ein Zehntel der Last, gegen die zweite Instanz mit Standard-`wal_sender_timeout` und ohne fremden Schreiber, samt Nullprobe ohne Leerlauf-Bestätigung; dieselbe Last gegen `wal_sender_timeout=2s` bindet `confirmed_flush_lsn` an das WAL-Ende) und der Sicherheits-Test (offene Quelltransaktion, Bestätigung hinter ihrem ersten Change, Stream-Neustart, Commit: der Change wird geliefert), im Paket `internal/bootstrap` der Verdrahtungs-Test mit dem realen ACK-Adapter und, nach dem Tier-Lauf gesondert auf der zweiten Instanz, `TestWALRetentionThresholdsFollowGrowthAtInactiveSlot` (beide Seiten der Schwelle an einem Slot, den kein Stream bestätigt; ohne Stream und ohne `Run`) | kein Gate, [`ADR-0030`](../docs/plan/adr/0030-testpyramide.md); DB-Adapter-Coverage ([`sensors/db-adapter-coverage.md`](sensors/db-adapter-coverage.md), [`ADR-0071`](../docs/plan/adr/0071-coverage-gate-messgegenstand-netzlos-pruefbare-flaeche.md)) |
| `make test-notify` | `natsnotify`-Adapter-Tests gegen einen echten NATS-Server im Testcontainer (gepinnter Digest, `tools/harness/run-notify-tests.sh`): realer Publish-Erfolgsbeleg über eine Subscription auf `cdc.changes.>` — Subjekt-Form und leerer Payload (`SPEC-017`); der Konstruktions-, Empty-Source- und Fehlerklassen-Wrapping-Teil läuft bereits ohne Server in `make test` (Verbindungsfehler bleiben auf Loopback, auch unter `--network none`) | kein Gate, [`ADR-0055`](../docs/plan/adr/0055-nats-change-notification-wecksignal.md), [`ADR-0030`](../docs/plan/adr/0030-testpyramide.md) · seit slice-052 |
| `make test-integration` | Compose-Integrationstest gegen die Compose-Umgebung (`compose.yaml`), inhaltlich über den ursprünglichen MVP-Zuschnitt hinausgewachsen: Umgebung frisch hochfahren → Schema-Rollout über d-migrate ([`ADR-0043`](../docs/plan/adr/0043-schemamigrationen-mit-d-migrate.md)) → Rollen-DSN-Verifikation direkt gegen die Compose-PostgreSQL-Instanz (`LH-QA-SEC-001`…`003`, [`ADR-0047`](../docs/plan/adr/0047-rollenspezifische-dsn-verdrahtung.md): ein cdc_reader-Login scheitert an einem schreibenden Aufruf, eine Login-Identität ohne REPLICATION-Attribut scheitert an einer Replication-Verbindung, eine cdc_capture-Login-Identität mit dem Attribut gelingt) → Feed-Container fährt die CDC-Runtime (ENV-Vertrag `CDC_CAPTURE_DSN`/`CDC_ADMIN_DSN`/`CDC_READER_DSN`/`CDC_SOURCE_ID`/`CDC_PUBLICATION`/`CDC_SLOT`/`CDC_TABLES` ist der Container-Vertrag in `compose.yaml`) → Toolchain-Container gegen das Compose-Netz; gepinnte Digests, DB-Daten im Container. Am Ende ein Black-Box-Rundlauf ([`ADR-0030`](../docs/plan/adr/0030-testpyramide.md) E2E-Tier): `register-consumer`/`acknowledge-consumer` laufen ausschließlich per `docker exec` gegen den laufenden Feed-Container (kein Go-Paket-Import), über einen simulierten Container-Neustart hinweg — Fortsetzen ab der bestätigten Position, gelesen über den bestehenden SQL-Lesezugriffsweg `cdc.changes`. Zusätzlich ein SQL-Administration-Live-Reload-Beleg ([`ADR-0050`](../docs/plan/adr/0050-sql-administration-antragsqueue-und-live-reload.md)): `SELECT cdc.enable_table(...)`/`cdc.disable_table(...)` gegen eine nicht in `CDC_TABLES` gelistete Tabelle, Poll auf `status = 'applied'`, dann realer Erfassungs- bzw. Nicht-Erfassungs-Beleg am **laufenden** Feed-Container — ohne `docker restart` · seit slice-037. Zusätzlich ein CLI-Diagnose-Beleg ([`LH-FA-SST-003`](../spec/lastenheft.md), deckt `LH-FA-ADM-002`…`005`): derselbe externe `docker exec`-Zugriffsweg gegen den neuen `diagnose`-Sondermodus, einmal im Normalbetrieb (Betriebsstatus, `cdc_capture_lag`, Consumer-Rückstand) und einmal mit einem direkt in `cdc.process_heartbeat` geschriebenen Fehlerzustand (`LH-FA-ADM-003`-Boundary: erkennbar von Normalbetrieb unterscheidbar) — ohne den laufenden Feed-Container zu beenden · seit slice-038. Zusätzlich ein isolierter Publication-Entzug-Wirksamkeitsbeleg (`LH-FA-CFG-002`): `ALTER PUBLICATION ... DROP TABLE` direkt per `psql`, ohne den regulären `cdc.disable_table`-Antragsweg, damit die App-seitige `Assembler`-Filterung als Alternativerklärung ausgeschlossen ist — belegt real, dass PostgreSQLs bereits laufende Decoding-Session die entzogene Tabelle sofort ausfiltert · seit slice-051. Zusätzlich ein HTTP-API-Rundlauf ([`LH-FA-SST-006`](../spec/lastenheft.md), [`ADR-0057`](../docs/plan/adr/0057-http-grpc-api.md)): ein Wegwerf-Client (`tools/harness/httpclient`) ruft `RegisterConsumer` mit dem `admin`-Token und `ListTables` mit dem `reader`-Token real per HTTP gegen den laufenden Feed-Container auf (`CDC_HTTP_ADDR`/`CDC_API_TOKEN_READER`/`CDC_API_TOKEN_ADMIN` im Container-Vertrag) — ein echter Netzwerk-Request über den Compose-Netz-Alias, kein `docker exec` und kein Mock; die reale Registrierung wird zusätzlich gegen `cdc.consumer` bestätigt · seit slice-061. Zusätzlich liest derselbe Wegwerf-Client (`tools/harness/httpclient`) über `GET /changes` mit dem `reader`-Token Changes im Bereich `[from, to)` um eine unmittelbar zuvor erfasste Zeile — geprüft am Inhalt (Tabelle, Operation, Row Image) und über ihre `change_id` gegen den Lesezugriffsweg `cdc.changes` gehalten ([`LH-FA-SST-006`](../spec/lastenheft.md), [`ADR-0081`](../docs/plan/adr/0081-changes-lesen-ueber-die-http-api.md)) · seit slice-087. Zusätzlich ein gRPC-Stream-Rundlauf ([`LH-FA-SST-008`](../spec/lastenheft.md), [`ADR-0060`](../docs/plan/adr/0060-grpc-streaming-mechanismus.md)): ein Wegwerf-Client (`tools/harness/grpcclient`) öffnet real über gRPC den Server-Stream gegen den laufenden Feed-Container (`CDC_GRPC_ADDR` im Container-Vertrag) und empfängt eine danach committete Änderung — belegt am Stream-Image über Tabelle, Operation und den Wert der Spalte `name`, und über ihre `change_id` gegen den Lesezugriffsweg `cdc.changes` gehalten (die Feldvollständigkeit des Nachrichtenschemas trägt `internal/adapters/driving/grpc/server_test.go` auf Unit-Ebene) —, während ein Stream-Öffnungsversuch ohne gültiges Token mit gRPC-Status `Unauthenticated` abgelehnt wird — ein echter Netzwerk-Request über den Compose-Netz-Alias, kein `docker exec` und kein Mock · seit slice-071. Zusätzlich zwei Struktur-/Verhaltens-Belege im Go-Testpaket (`test/integration/integration_test.go`): `TestE2ESchemaChangeDropColumn` (`LH-FA-SCH-003`, [`ADR-0063`](../docs/plan/adr/0063-lh-fa-sch-003-testform-korrektur.md) Supersedes [`ADR-0058`](../docs/plan/adr/0058-testansatz-fuenf-luecken.md) Entscheidung 1) — eine eigene, wegwerfbare Spalte auf `feed_e2e_schema` real per `ALTER TABLE … ADD/DROP COLUMN` entfernt; die reale Entfernung löst denselben `relationOther`/`ErrIncompatibleSchemaChange`-Pfad aus wie eine inkompatible Typänderung (`LH-FA-SCH-004`, Konvergenz bereits von `ADR-0059` Teilfrage 4 akzeptiert) und beendet den Erfassungspfad des Feed-Containers dauerhaft sichtbar über `error_class=schema` — die zuvor erfasste Änderung bleibt über `cdc.changes` inklusive historischem Wert unverändert lesbar (Boundary); läuft deshalb als eigener Aufruf nach der ursprünglichen Container-Ende-Grenze, mit explizitem Replication-Slot-Neuanlage/Schema-Version-Nachtrag/Neustart/Health-Poll davor — und `TestE2EChangeTableMetadataExtensibility` (`LH-FA-DAT-006`, [`ADR-0058`](../docs/plan/adr/0058-testansatz-fuenf-luecken.md) Entscheidung 2) — ein reales `ALTER TABLE cdc.change ADD COLUMN` auf dem internen Change-Schema; vor und nach der Erweiterung erfasste Zeilen bleiben über `cdc.changes` lesbar, die neue Spalte ist über die View nicht sichtbar (explizite `columns:`-Signatur, `tools/schema/schema.yaml`), `t.Cleanup` nimmt sie real zurück · seit slice-062. Zusätzlich ein Upgrade-Sicherheits-Rundlauf (`LH-QA-OPS-005`, [`ADR-0064`](../docs/plan/adr/0064-lh-qa-ops-005-testansatz-korrektur.md) Supersedes [`ADR-0058`](../docs/plan/adr/0058-testansatz-fuenf-luecken.md) Entscheidung 3): ein realer Container-Tausch über `$COMPOSE up -d --force-recreate --no-deps pg-change-feed` ersetzt den Feed-Container durch eine neue Instanz desselben `:dev`-Images (`container_name` bleibt `cdc-test-feed`) — `postgres`/`nats` bleiben durch `--no-deps` unberührt und healthy; Health-Poll wie beim bestehenden simulierten Neustart-Rundlauf, der vor dem Tausch erfasste Datenstand bleibt über `cdc.changes` identisch lesbar, eine danach eingefügte Zeile wird weiterhin erfasst · seit slice-063. Zusätzlich ein Spaltenausschluss-Rundlauf ([`LH-FA-CFG-005`](../spec/lastenheft.md), [`ADR-0059`](../docs/plan/adr/0059-spaltenauswahl-mechanismus.md); trägt zugleich `LH-QA-SEC-004`, dessen Messmethode ausschließlich über `LH-FA-CFG-005` läuft): eine eigene, nicht in `CDC_TABLES` gelistete Tabelle wird über `cdc.enable_table` aktiviert, `SELECT cdc.exclude_column(...)` gegen die laufende Aktivierung wird real verarbeitet (Poll auf `status = 'applied'`), die danach erfasste Change trägt den ausgeschlossenen Spaltenschlüssel nicht mehr im Row Image und den Wert nirgends, während die nicht ausgeschlossene Spalte darin bleibt und die vor dem Ausschluss erfasste Change unverändert lesbar bleibt; ein `cdc.exclude_column` gegen eine nicht existierende Spalte landet real `failed` samt Fehlertext — alles am **laufenden** Feed-Container, ohne Neustart · seit slice-068. Zusätzlich ein SSE-Stream-Rundlauf ([`LH-FA-SST-008`](../spec/lastenheft.md), [`ADR-0061`](../docs/plan/adr/0061-http-sse-zusaetzlich-zu-grpc.md)): ein Wegwerf-Client (`tools/harness/sseclient`) öffnet real per HTTP den Endpunkt `GET /changes/stream` gegen den laufenden Feed-Container (`CDC_HTTP_ADDR` im Container-Vertrag) und empfängt eine danach committete Änderung — belegt am Event über Tabelle, Operation und den Wert der Spalte `name`, und über ihre `change_id` gegen den Lesezugriffsweg `cdc.changes` gehalten (die Feldvollständigkeit des Nachrichtenschemas trägt `internal/adapters/driving/http/sse_test.go` auf Unit-Ebene) —, während ein Aufruf ohne gültiges Token mit HTTP-Status `401` abgelehnt wird; ein echter Netzwerk-Request über den Compose-Netz-Alias, kein `docker exec` und kein Mock · seit slice-072. Zusätzlich ein Spaltenausschluss-Neustart-Beleg ([`ADR-0065`](../docs/plan/adr/0065-spaltenausschluss-dauerhafter-traeger.md), trägt `LH-QA-SEC-004`): nach einem **realen** `docker restart` des Feed-Containers leitet der Prozessstart den dauerhaften Ausschlussstand aus den `applied`-Zeilen der beiden Spalten-Antragsarten ab ([`LH-FA-CFG-005`](../spec/lastenheft.md) über dieselbe Tabelle wie im Spaltenausschluss-Rundlauf) — die danach erfasste Change trägt den ausgeschlossenen Spaltenschlüssel nicht mehr im Row Image und den Wert nirgends, während die nicht ausgeschlossene Spalte darin bleibt; der Beleg läuft vor der Container-Ende-Grenze und vor dem Upgrade-Tausch · seit slice-075. Zusätzlich ein NATS-Vollinhalts-Stream-Rundlauf ([`LH-FA-SST-008`](../spec/lastenheft.md), [`ADR-0100`](../docs/plan/adr/0100-nats-dritter-vollinhalts-zustellweg.md)): ein Wegwerf-Client (`tools/harness/natsstreamsub`) verbindet sich real per NATS mit dem gültigen `CDC_NATS_STREAM_TOKEN` gegen den laufenden Feed-Container (`CDC_NATS_URL` im Container-Vertrag, Server-Auth über `--auth`) und empfängt eine danach committete Änderung als vollständiges JSON-Event über `cdc.stream.<source_id>.<schema>.<table>` — belegt am Event über Tabelle, Operation und den Wert der Spalte `name`, und über ihre `change_id` gegen den Lesezugriffsweg `cdc.changes` gehalten (die Feldvollständigkeit des Nachrichtenschemas trägt `internal/adapters/driven/natsstream/publisher_test.go` auf Unit-Ebene) —, während ein Verbindungsversuch ohne Token und einer mit falschem Token vom NATS-Server abgelehnt werden; das bestehende Wecksignal (`tools/harness/natssub`) funktioniert mit demselben Test-Token unverändert weiter — ein echter NATS-Request über den Compose-Netz-Alias, kein `docker exec` und kein Mock · seit slice-nats-drittstream-core. Zusätzlich sieben Backfill-Rundläufe ([`LH-FA-CAP-009`](../spec/lastenheft.md), [`ADR-0111`](../docs/plan/adr/0111-backfill-bestand-snapshot-bulk-copy.md)) am **laufenden** Feed-Container, ausschließlich über SQL-Funktionen und Views, HTTP, `docker exec` und `docker kill`: Happy Path (Bestand einer aktivierten Tabelle über `cdc.changes` und `GET /changes` als `INSERT` mit `origin = 'backfill'`, unterscheidbar von einer danach erfassten WAL-Änderung derselben Zeile), Schema-Version ([`ADR-0116`](../docs/plan/adr/0116-backfill-schema-version-referenz-reichweite.md): die Kennung der Backfill-Changes ist die zum Antrag aktuelle Version, das Bild trägt eine danach hinzugefügte Spalte), Startposition eines frisch registrierten Consumers (über den CLI-Weg registriert, über `GET /consumers/position` und `cdc.consumer_status` gemessen), Boundary (leere Tabelle, zweiter Antrag bei aktivem Run), Replay-Invariante (`TestE2EBackfillReplayInvariant`: nebenläufige Schreiber vor und nach der Snapshot-Position, das Log ab dem Log-Anfang ergibt den Quellstand), DDL-Fenster (Spaltenentfernung zwischen Snapshot-Export und -Import endet den Run `failed` mit Klasse `storage`; ein Umschreiben der Tabelle in demselben Fenster, `ALTER COLUMN … TYPE`, endet ihn `failed` mit Klasse `transient` ([`ADR-0118`](../docs/plan/adr/0118-backfill-umschreiben-im-snapshot-fenster.md)), ein neuer Antrag übernimmt danach alle Zeilen) und Negative (`docker kill` im zweiten Block: keine sichtbare Change, kein Slot und keine Sitzung, Run `interrupted`, erneuter Antrag übernimmt den Bestand einmal und vollständig, ein `queued` wartender Run wird beim Prozessstart aufgenommen); die Zeitpunkte der Runs halten eine offene Schreibtransaktion (Slot-Anlage), ein unbestätigter Schlüssel in `cdc.transaction` (zweiter Block) und `docker pause` (Fenster nach dem Export; die Pause bleibt unter der Hälfte von `wal_sender_timeout`). Zusätzlich ein Leerlauf-Bestätigungs-Rundlauf ([`LH-FA-CAP-009`](../spec/lastenheft.md), [`LH-QA-REL-001`](../spec/lastenheft.md), [`ADR-0120`](../docs/plan/adr/0120-capture-slot-leerlauf-bestaetigung.md)): der Feed-Container läuft mit einer Konfigurationsdatei (`CDC_CONFIG_FILE`, `wal_retention_warn_bytes`/`wal_retention_error_bytes` 4 MiB/8 MiB), die eine Compose-Override-Datei im Temp-Verzeichnis des Runners an den Dienst hängt (`compose.yaml` bleibt unverändert, der Container wird am Phasen-Ende ohne Override wiederhergestellt); ein Backfill-Run über 30.000 Zeilen, dessen WAL die Fehlerschwelle übersteigt (der Runner liest das WAL des Runs und bricht ab, wenn es sie nicht übersteigt), und ein Schreiber auf eine nicht aktivierte Tabelle mit ebenfalls größerem WAL lassen den Container laufen (je 12 s beobachtet, Startzeitpunkt unverändert), der Run endet `completed`, und der Bestand ist über `cdc.changes` lesbar · seit slice-backfill-slot-leerlauf-bestaetigung. Zusätzlich schreibt der Lauf `docs/user/e2e-abdeckung.md` — die E2E-Abdeckungstabelle je Spec-Kennung, abgeleitet aus dem Quelltext der `func TestE2E*` und den Phasen-Deklarationen des Runners (Erzeugnis, **kein** Lauf-Beleg) · seit slice-074 | kein Gate, [`ADR-0030`](../docs/plan/adr/0030-testpyramide.md), [`LH-QA-POR-003`](../spec/lastenheft.md) |
| `.github/workflows/upstream-drift.yml` | automatisiert das vollständige Neun-Achsen-Pin-Inventar P1–P9 ([`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Entscheidung 7): `make image-stale` (P1/P2) sowie die sieben `make pin-stale-*`-Targets (P3–P9), je Achse ein eigener Schritt mit `if: always()` (fail-open — ein Werkzeug-/Netzausfall EINER Achse führt nur zum Rot dieses einen Schritts, nicht zum Abbruch der übrigen); `schedule` (nächtlich, versetzt zu `image-scan.yml`) + `workflow_dispatch`, kein Pull-Request-/Push-Trigger. Advisory: ein roter Lauf ist sichtbar, blockiert aber kein Gate und keinen anderen Workflow | kein Gate, [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Entscheidung 7 |
| `.github/workflows/e2e.yml` | automatisiert `make image` gefolgt von `make test-integration` auf jeden Pull Request und Push (derselbe Trigger wie `ci.yml`) — läuft aber als **eigener, nicht-blockierender** Workflow statt als Job in `ci.yml`: ein roter Lauf ist im PR sichtbar, blockiert aber keinen Merge (kein Required-Status-Check — das ist eine Repository-Einstellung, keine Workflow-Eigenschaft), damit der deutlich längere Compose-Stack-Aufbau `ci.yml`s kurzes Signal nicht verzögert. `make image` läuft zuerst, weil `compose.yaml` das lokal gebaute Image ohne eigenen `build:`-Block referenziert ([`ADR-0044`](../docs/plan/adr/0044-image-beleg-semantik.md)). Seit slice-064 trägt der Job eine `strategy: matrix:` über die beiden in `SPEC-012` festgelegten PostgreSQL-Digests (`LH-QA-POR-001`, [`ADR-0058`](../docs/plan/adr/0058-testansatz-fuenf-luecken.md) Entscheidung 4) — `fail-fast: false`, beide Legs laufen parallel und unabhängig sichtbar; jedes Leg setzt `PG_TEST_IMAGE` auf Job-Ebene, `compose.yaml`s `${PG_TEST_IMAGE}`-Interpolation liest die Variable, `make image`/`make test-integration` laufen unverändert je Leg | kein Gate, [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md), [`ADR-0058`](../docs/plan/adr/0058-testansatz-fuenf-luecken.md), [`LH-QA-POR-001`](../spec/lastenheft.md), [`LH-QA-POR-003`](../spec/lastenheft.md) |
| `.github/workflows/release.yml` | Release-Workflow ([`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Entscheidung 3), Trigger ausschließlich `push: tags: ['v*']` (`ci.yml`/`e2e.yml` schließen Tag-Pushes per `tags-ignore` aus, kein Doppellauf): validiert den Tag strikt gegen SemVer 2.0, gleicht ihn gegen `docs/user/version.md` am getaggten Commit ab (Abbruch bei Abweichung, vor jedem Login/Build/Push), baut über `make image VERSION=<Version>` (ein Build), pusht nach GHCR **und** Docker Hub (Content-Mirror), setzt `:latest` auf beiden Registries nur bei einem stabilen (nicht-Prerelease) Tag (`LATEST=true`), legt abschließend über die auf dem Runner vorinstallierte `gh`-CLI ein GitHub-Release mit dem Image-Digest im Beschreibungstext an. Nach einem erfolgreichen `release`-Job ruft der zusätzliche `hub-description`-Job (`needs: release`) `hub-description.yml` per `uses:`/`workflow_call` auf (`secrets: inherit`, [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Entscheidung 8) — ein Fehlschlag dort lässt den bereits abgeschlossenen `release`-Job unberührt | kein Gate, [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) |
| `.github/workflows/hub-description.yml` | synchronisiert die Docker-Hub-Repository-Beschreibung von `pt9912/pg-change-feed` mit `README.md` — reiner curl/jq-Aufruf auf dem Runner gegen die öffentliche Docker-Hub-API (`POST /v2/auth/token` mit `{identifier, secret}` liefert ein `access_token`, `PATCH /v2/repositories/<namespace>/<repo>` mit `{full_description}` aktualisiert die Beschreibung; real gegen die echte API mit ungültigen Zugangsdaten geprüft — 400/401 bestätigen Endpoint, Feldnamen und dass `pt9912/pg-change-feed` dort existiert), kein gepinnter Action-Fork nötig. Zwei Trigger: `workflow_call` (aus `release.yml`, `needs: release`) und `workflow_dispatch` (manueller Nachlauf, z. B. nach einer reinen `README.md`-Änderung). `DOCKERHUB_USERNAME`/`DOCKERHUB_TOKEN` sind Repository-Secrets, die dieser Workflow referenziert, aber nicht anlegt; der volle Erfolgspfad mit realen Zugangsdaten bleibt bis zum ersten echten Release unbewiesen | kein Gate, [`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md) Entscheidung 8 |
| `.github/workflows/sdk-csharp-release.yml` | NuGet.org-Publish-Workflow für das C#-SDK-Package `PgChangeFeed.Client` ([`LH-FA-SST-009`](../spec/lastenheft.md), [`ADR-0106`](../docs/plan/adr/0106-csharp-nuget-erstes-sdk-package.md) Festlegung 4), Trigger ausschließlich `push: tags: ['sdk-csharp-v*']` — ein eigener Tag-Namensraum, getrennt vom Server-Release-Raum `v*` (`ci.yml`/`e2e.yml` schließen jeden Tag-Push per `tags-ignore: ['**']` aus, `release.yml` reagiert ausschließlich auf `push: tags: ['v*']` — `sdk-csharp-v*` matcht keines der drei Muster, kein Doppellauf): validiert den Tag strikt gegen `sdk-csharp-v<SemVer 2.0>` (`tools/harness/sdk-csharp-release-tag-info.sh`, netzlos testbar über `make test-sdk-csharp-release-tag-info` — teilt die SemVer-2.0-Regex mit `tools/harness/release-tag-info.sh` über `tools/harness/semver-regex.sh`, damit beide Stellen nicht unabhängig voneinander driften), gleicht ihn gegen die in `sdks/csharp/PgChangeFeed.Client/PgChangeFeed.Client.csproj` geführte `<Version>` ab (Abbruch bei Abweichung, vor jedem Build/Push), baut/testet/paketiert danach Docker-only über `make sdk-pack-csharp` und veröffentlicht das erzeugte `.nupkg` per `dotnet nuget push … --api-key $NUGET_API_KEY --source https://api.nuget.org/v3/index.json` — über die auf dem Runner vorinstallierte `dotnet`-CLI (reine Workflow-Mechanik, kein `make`-Target zu duplizieren, analog `release.yml`s `gh`-CLI-Nutzung). Kein `:latest`-Äquivalent (NuGet kennt keins), kein GitHub-Release-Eintrag für das SDK. `NUGET_API_KEY` ist ein Repository-Secret, das dieser Workflow referenziert, aber nicht anlegt (externe Kontohandlung, dieselbe Klasse wie `DOCKERHUB_USERNAME`/`DOCKERHUB_TOKEN`) — sein Vorhandensein und der reale Post-Push-Erfolg bleiben bis zum ersten echten Tag-Push unbewiesen (`AGENTS.md` §3.10) | kein Gate, [`ADR-0106`](../docs/plan/adr/0106-csharp-nuget-erstes-sdk-package.md) Festlegung 4 · seit slice-sdk-csharp-publish-workflow |
| `.github/workflows/sdk-python-release.yml` | PyPI-Publish-Workflow für das Python-SDK-Package `pgchangefeed` ([`LH-FA-SST-009`](../spec/lastenheft.md), [`ADR-0107`](../docs/plan/adr/0107-python-pypi-zweites-sdk-package.md) Festlegung 5, [`ADR-0108`](../docs/plan/adr/0108-python-sdk-uv-statt-build-twine.md) §Entscheidung Festlegung 1), Trigger ausschließlich `push: tags: ['sdk-python-v*']` — ein eigener Tag-Namensraum, getrennt vom Server-Release-Raum `v*` und vom C#-SDK-Raum `sdk-csharp-v*` (`ci.yml`/`e2e.yml` schließen jeden Tag-Push per `tags-ignore: ['**']` aus, `release.yml` reagiert ausschließlich auf `push: tags: ['v*']`, `sdk-csharp-release.yml` ausschließlich auf `push: tags: ['sdk-csharp-v*']` — `sdk-python-v*` matcht keines der drei Muster, kein Doppellauf): validiert den Tag strikt gegen den in `ADR-0107` Festlegung 4 gewählten einfachen PEP-440-Fall `sdk-python-v<MAJOR.MINOR.PATCH>` (`tools/harness/sdk-python-release-tag-info.sh`, netzlos testbar über `make test-sdk-python-release-tag-info` — eigenständige Regex, **kein** Sourcing aus `tools/harness/semver-regex.sh`, weil PEP 440 nicht identisch mit SemVer 2.0 ist), gleicht ihn gegen die in `sdks/python/pgchangefeed/pyproject.toml` geführte `[project] version` ab (Abbruch bei Abweichung, vor jedem Build/Push), baut/testet/paketiert danach Docker-only über `make sdk-pack-python` und veröffentlicht die erzeugten Artefakte (`.whl`+`.tar.gz`) per `uv publish` (Token über `UV_PUBLISH_TOKEN`, kein `-u __token__ -p`-Flag-Paar wie bei `twine`). `uv` ist auf dem Runner nicht vorinstalliert — bezogen über die SHA-gepinnte Action `astral-sh/setup-uv`, mit demselben Versionsstand (`0.12.17`), den `sdks/python/Dockerfile` per digest-gepinntem `COPY --from=` für den Docker-only-Pack-Schritt nutzt. Kein `:latest`-Äquivalent (PyPI kennt keins), kein GitHub-Release-Eintrag für das SDK. `PYPI_API_TOKEN` ist ein Repository-Secret, das dieser Workflow referenziert, aber nicht anlegt (externe Kontohandlung, dieselbe Klasse wie `NUGET_API_KEY`/`DOCKERHUB_USERNAME`/`DOCKERHUB_TOKEN`) — real geprüft (`gh secret list`) existiert es zum Zeitpunkt dieses Zuges **nicht**; sein Vorhandensein und der reale Post-Push-Erfolg bleiben bis zum ersten echten Tag-Push unbewiesen (`AGENTS.md` §3.10) | kein Gate, [`ADR-0107`](../docs/plan/adr/0107-python-pypi-zweites-sdk-package.md) Festlegung 5, [`ADR-0108`](../docs/plan/adr/0108-python-sdk-uv-statt-build-twine.md) §Entscheidung Festlegung 1 · seit slice-sdk-python-publish-workflow |
| `.github/workflows/sdk-kotlin-release.yml` | GitHub-Packages-Publish-Workflow für das Kotlin-SDK-Package `pgchangefeed-kotlin` ([`LH-FA-SST-009`](../spec/lastenheft.md), [`ADR-0109`](../docs/plan/adr/0109-kotlin-github-packages-drittes-sdk-package.md) Festlegung 2/5), Trigger ausschließlich `push: tags: ['sdk-kotlin-v*']` — ein eigener Tag-Namensraum, getrennt vom Server-Release-Raum `v*` und den beiden anderen SDK-Räumen `sdk-csharp-v*`/`sdk-python-v*` (`ci.yml`/`e2e.yml`/`examples.yml` schließen jeden Tag-Push per `tags-ignore: ['**']` aus, `release.yml`/`sdk-csharp-release.yml`/`sdk-python-release.yml` reagieren ausschließlich auf ihre je eigenen `tags:`-Muster — `sdk-kotlin-v*` matcht keines der fünf Muster, kein Doppellauf): validiert den Tag strikt gegen `sdk-kotlin-v<SemVer 2.0>` (`tools/harness/sdk-kotlin-release-tag-info.sh`, netzlos testbar über `make test-sdk-kotlin-release-tag-info` — teilt die SemVer-2.0-Regex mit `tools/harness/release-tag-info.sh`/`tools/harness/sdk-csharp-release-tag-info.sh` über `tools/harness/semver-regex.sh`), gleicht ihn gegen die Top-Level-`version`-Zeile in `sdks/kotlin/pgchangefeed-kotlin/build.gradle.kts` ab (Abbruch bei Abweichung, vor jedem Build/Publish), baut/testet/paketiert danach Docker-only über `make sdk-pack-kotlin` und veröffentlicht anschließend ebenfalls Docker-only über eine eigene `publish`-Docker-Stufe (`sdks/kotlin/Dockerfile`, baut auf der bereits vorhandenen `build`-Stufe auf, [`ADR-0109`](../docs/plan/adr/0109-kotlin-github-packages-drittes-sdk-package.md) Festlegung 5 wörtlich: Docker-only bis einschließlich `publish`) — `docker build --build-context proto=proto --target publish` gefolgt von `docker run --rm -e GITHUB_ACTOR=… -e GITHUB_TOKEN=… <image> ./gradlew --no-daemon publish`, mit Netzwerkzugriff zur Laufzeit (kein `--network none`, anders als jede andere Stufe dieses Dockerfiles), die `.proto`-Quelle (`proto/cdc/stream/v1/changestream.proto`) fließt dabei über denselben benannten Bau-Kontext `proto` ein wie bei `make sdk-pack-kotlin`, kein manueller Kopier-Schritt außerhalb von Docker (Korrektur von Review-Finding F-1, `docs/reviews/review-slice-sdk-kotlin-publish-workflow.md` — eine frühere Fassung rief `./gradlew publish` direkt auf dem Runner auf). Auth **ausschließlich** über das eingebaute `GITHUB_TOKEN` (`permissions: contents: read` / `packages: write` auf Job-Ebene, real dokumentiertes Minimalrezept) — **kein** externes Repository-Secret, der zentrale Unterschied zu `sdk-csharp-release.yml`/`sdk-python-release.yml`. `GITHUB_ACTOR` ist ein automatisch bereitgestellter Default-Umgebungswert; `GITHUB_TOKEN` wird aus `secrets.GITHUB_TOKEN` als Umgebungsvariable gesetzt, gelesen von `sdks/kotlin/pgchangefeed-kotlin/build.gradle.kts`s `publishing.repositories.maven.credentials`. Kein `:latest`-Äquivalent (GitHub Packages kennt keins), kein GitHub-Release-Eintrag für das SDK. Der reale Post-Push-Erfolg bleibt bis zum ersten echten Tag-Push unbewiesen (`AGENTS.md` §3.10) | kein Gate, [`ADR-0109`](../docs/plan/adr/0109-kotlin-github-packages-drittes-sdk-package.md) Festlegung 2/5 · seit slice-sdk-kotlin-publish-workflow |
| `make schema-validate` | prüft das neutrale Schema-YAML mit d-migrate (netzlos; Vorlauf vor jedem `generate`/`migrate`) | kein Gate, [`ADR-0043`](../docs/plan/adr/0043-schemamigrationen-mit-d-migrate.md) |
| `make bench` | Performance-Benchmark-Infrastruktur (`ADR-0054` §(b), Pass/Fail-Schwellen `ADR-0104`): vier eigenständige Bench-Skripte hinter einem Ziel (drei mit Schwelle, eines ohne), je eine eigene, von `tools/harness/run-integration-tests.sh` unabhängige PostgreSQL-/Feed-Umgebung (`docker network`/`docker run`, kein `compose.yaml`-Bezug) samt Schema-Rollout über d-migrate. `tools/bench-source-impact.sh` (`LH-QA-PER-001`): dieselbe Insert-Last (N=5000 Zeilen, Median von 5 Läufen je Phase) einmal ohne jeden Replication-Slot, einmal mit aktivem Slot und laufendem Feed-Container — scheitert, wenn der Overhead [`SPEC-025`](../spec/pflichtenheft.md)s 35-%-Schwelle überschreitet (25,0 %/28,2 % über zwei unabhängige Läufe, **übernommen** aus [`ADR-0104`](../docs/plan/adr/0104-benchmark-schwellen-per-001-002-003.md), Host dort nicht genannt; der Ausgang hängt an der Latenz des Festschreibens auf dem Datenträger des Messhosts — auf dem Host von `slice-backfill-bench-richtgroesse` (Linux 6.8.0-139-generic, `pg_test_fsync` `fdatasync` 2.956 µs je Operation) liegt der Overhead bei 87,5 % bis 95,8 % (sechs Läufe: 87,5 %, 93,9 % und 95,8 % **übernommen** aus dem Verdikt, 94,6 % im Verdikt gemessen, 91,7 % **übernommen** aus dem Plan des Slice, 92,0 % im Lauf des Verifikations-Reports `verifikation-slice-backfill-bench-richtgroesse` §1 gemessen), die Schwelle bleibt 35 %, und `make bench` endet dort an diesem Skript rot; Ursache und Messungen: [`architect-verdict-backfill-wal-rueckstand-und-bench-rot`](../docs/reviews/architect-verdict-backfill-wal-rueckstand-und-bench-rot.md)). `tools/bench-scaling.sh` (`LH-QA-PER-002`): durchläuft real die drei `SPEC-014`-Lastenstufen (klein/mittel/groß) gegen einen aktiven Feed-Container, dokumentiert erreichten Durchsatz — scheitert, wenn `cdc_capture_lag` bei irgendeiner Stufe [`SPEC-013`](../spec/pflichtenheft.md)s bestehende 60-s-Fehlergrenze überschreitet (real gemessen: ≈1s bei allen drei Stufen) — Default-Modus fährt stark verkürzte Dauern bei unveränderter Ziel-Rate, `--full` fährt die tatsächlichen `SPEC-014`-Dauern. `tools/bench-batch-vs-single.sh` (`LH-QA-PER-003`): Batch- vs. Einzelabruf beim Lesen über `cdc.changes` — scheitert, wenn der Batch-Vorteil unter `SPEC-025`s 10×-Mindestwert fällt (real gemessen: 194×), schreibt danach `docs/user/bench-abdeckung.md` (macht `LH-QA-PER-001`/`002`/`003` für `make doc-trace` sichtbar, `trace.coverage`-Zweiteintrag in `.d-check.yml`). `tools/bench-backfill.sh` ([`LH-FA-CAP-009`](../spec/lastenheft.md), [`ADR-0111`](../docs/plan/adr/0111-backfill-bestand-snapshot-bulk-copy.md), [`ADR-0113`](../docs/plan/adr/0113-backfill-rollenschnitt-aufnahme-warnkriterium.md)): eine **Messung ohne Pass/Fail-Schwelle** — Kopierdauer eines Backfill-Runs je Tabellengröße (Median über mehrere Läufe), Abweichung der geschätzten Zeilenzahl, Speicher des Feed-Containers, WAL-Rückstand des Capture-Slots samt vom Slot gehaltenem WAL je Run (Spitze im Run, Rückstand unmittelbar danach und nach einer Wartezeit ohne Schreibzugriff, nach der letzten Stufe die höchste Spitze der größten Stufe im Vergleich zur Warnschwelle von `SPEC-013`) und `cdc_capture_lag` während eines Runs; aus der Rate folgt die Richtgröße (abgeleitet), die im Code und im Handbuch steht; sie schreibt keine Zeile nach `docs/user/bench-abdeckung.md`, weil diese Datei nur durchgesetzte Schwellen bindet. Vertrag, Phasen und Grenzen: [`harness/targets/bench-backfill.md`](targets/bench-backfill.md). `make bench` bleibt außerhalb von `make gates`/`fullbuild` — braucht `make image` und DB-Zugang, kein netzloser Lauf | kein Gate, [ADR-0054](../docs/plan/adr/0054-coverage-gate-und-benchmark-infrastruktur.md) §(b), [ADR-0104](../docs/plan/adr/0104-benchmark-schwellen-per-001-002-003.md) |
| `make schema-rollout` | rollt das neutrale Schema mit d-migrate aus: Precheck (`--plan-only`), Wache `tools/schema/rolloutguard`, Vorlauf `DROP VIEW cdc.<name>` bei einer View-Signatur-Änderung, `schema migrate --execute` mit Pflicht-Report (`tools/schema/plan.yaml`) und Rollback-Artefakt (`tools/schema/down.sql`), danach die vier psql-Schritte `tools/schema/nacharbeit-*.sql`; braucht DB-Zugang, ein zweiter Lauf gegen ein migriertes Ziel endet mit Exit 0. Belegt durch `tools/harness/run-schema-rollout-guard-test.sh` (sechs Läufe gegen eine Wegwerf-PostgreSQL) und die Unit-Tests der Wache (`make test`) | kein Gate, [`harness/targets/schema-rollout.md`](targets/schema-rollout.md), [`ADR-0043`](../docs/plan/adr/0043-schemamigrationen-mit-d-migrate.md), [`ADR-0114`](../docs/plan/adr/0114-schema-rollout-vorlauf-view-signatur.md) |
| `make examples-csharp` | baut die vier Werkzeugketten-Images der C#-Sprach-Wurzel (Bau-Kontext `examples/csharp/`, eigenes digest-gepinntes Dockerfile, gemeinsame `build`-Stufe) — `pg-change-feed-examples:csharp` (http-client), `pg-change-feed-examples:csharp-sse` (sse-client), `pg-change-feed-examples:csharp-nats` (nats-client) und `pg-change-feed-examples:csharp-grpc` (grpc-client, seit slice-102) — und fährt darin `dotnet restore`/`build`/`test` für alle vier Programme; ein roter Test bricht den `docker build` ab; die Beispiele bleiben Doku mit Bau-Bindung, kein Lauf-Beleg. Für grpc-client liest der Bau die `.proto` zusätzlich über einen benannten Bau-Kontext (`--build-context proto=proto`, `COPY --from=proto …`) — jeder der vier Aufrufe trägt ihn zwingend, ohne ihn bricht der Bau an dieser Stelle ab. Braucht Netz (NuGet-Paketbezug), deshalb Werkzeug statt Gate | kein Gate, [`ADR-0087`](../docs/plan/adr/0087-beispiel-clients-csharp-kotlin.md), [`ADR-0090`](../docs/plan/adr/0090-beispiel-clients-volle-matrix.md) · seit slice-098, erweitert seit slice-100, seit slice-101, seit slice-102 |
| `make examples-kotlin` | baut die vier Werkzeugketten-Images der Kotlin-Sprach-Wurzel (Bau-Kontext `examples/kotlin/`, eigenes digest-gepinntes Dockerfile, gemeinsame `build`-Stufe) — `pg-change-feed-examples:kotlin` (http-client), `pg-change-feed-examples:kotlin-sse` (sse-client), `pg-change-feed-examples:kotlin-nats` (nats-client) und `pg-change-feed-examples:kotlin-grpc` (grpc-client, seit slice-103) — und fährt darin den Gradle-Wrapper (`test`/`installDist`) für alle vier Module; ein roter Test bricht den `docker build` ab; die Beispiele bleiben Doku mit Bau-Bindung, kein Lauf-Beleg. Für grpc-client liest der Bau die `.proto` zusätzlich über einen benannten Bau-Kontext (`--build-context proto=proto`, `COPY --from=proto …`) — jeder der vier Aufrufe trägt ihn zwingend, ohne ihn bricht der Bau an dieser Stelle ab. Braucht Netz (Maven-Central-/Gradle-Plugin-Portal-Paketbezug), deshalb Werkzeug statt Gate | kein Gate, [`ADR-0087`](../docs/plan/adr/0087-beispiel-clients-csharp-kotlin.md), [`ADR-0090`](../docs/plan/adr/0090-beispiel-clients-volle-matrix.md) · seit slice-099, erweitert seit slice-100, seit slice-101, seit slice-103 |
| `make example-run-go` | baut bei Bedarf (Docker-Layer-Cache) und startet real eines der vier Go-Beispiele — Pflicht-Argument `SURFACE=http\|sse\|grpc\|nats`, optional `ARGS="…"` für die Flag-Übersteuerung je Beispiel; ein fehlendes/unbekanntes `SURFACE` bricht mit `$(error …)` ab, bevor `docker build`/`docker run` läuft. Bau-Quelle ist `examples/Dockerfile` (Bau-Kontext Repo-Wurzel, isoliert über `examples/Dockerfile.dockerignore`, digest-gepinnte Basis wiederverwendet aus der Wurzel-`Dockerfile`); der Start ist `docker run --rm --network cdc-examples --env-file examples/.env <image-tag> $(ARGS)` — das Docker-Netzwerk `cdc-examples` und `examples/.env` legt `slice-beispiele-compose-bootstrap` an, dieses Ziel referenziert beide nur. Analog dazu, aber ohne eigenen Bau-Schritt: `example-run-csharp`/`example-run-kotlin` (siehe Zeile darunter). Braucht das benannte Docker-Netzwerk, deshalb Werkzeug statt Gate | kein Gate, [`ADR-0098`](../docs/plan/adr/0098-beispiel-clients-start-ueber-make-dockerfile.md) |
| `make example-run-csharp`/`make example-run-kotlin` | startet real eines der vier bereits gebauten C#- bzw. Kotlin-Beispiele — Pflicht-Argument `SURFACE=http\|sse\|grpc\|nats`, optional `ARGS="…"` für die Flag-Übersteuerung je Beispiel; ein fehlendes/unbekanntes `SURFACE` bricht mit `$(error …)` ab, bevor `docker run` läuft. Anders als `example-run-go` bauen diese Ziele **nichts** — der Bau liegt bei `make examples-csharp`/`make examples-kotlin` (`ADR-0087`/`ADR-0090`, unverändert); der Start ist `docker run --rm --network cdc-examples --env-file examples/.env pg-change-feed-examples:csharp[-<surface>]`/`:kotlin[-<surface>] $(ARGS)` gegen den bereits vorhandenen Image-Tag — läuft der Bau-Schritt noch nicht, scheitert `docker run` real und sichtbar mit einem Docker-eigenen „image not found", kein stiller Vorab-Bau. Das Docker-Netzwerk `cdc-examples` und `examples/.env` legt `slice-beispiele-compose-bootstrap` an, dieses Ziel referenziert beide nur. Braucht das benannte Docker-Netzwerk, deshalb Werkzeug statt Gate | kein Gate, [`ADR-0098`](../docs/plan/adr/0098-beispiel-clients-start-ueber-make-dockerfile.md) · seit slice-beispiele-csharp-kotlin-start-target |
| `make example-demo-up`/`make example-demo-down` | fährt die Demo-/Quickstart-Umgebung unter `examples/` real hoch bzw. räumt sie ab (`examples/compose.yaml`, eigenes Docker-Netzwerk `cdc-examples`, eigenständig von der Wurzel-`compose.yaml` — beide können gleichzeitig laufen, real geprüft): PostgreSQL+NATS starten, Schema-Rollout über d-migrate treiben (`tools/schema/apply-rollout.sh`-Muster, `examples/bootstrap.sh`), eine Beispiel-Quelle (`cdc.source`) und -Tabelle (`public.orders`, eine Demo-Zeile) registrieren, dann den Feed-Container starten — der aktiviert `public.orders` beim eigenen Start automatisch über `CDC_TABLES` (`ADR-0028`). Die Demo-Zeile wird erst **nach** Anlage des Replication-Slots eingefügt (logische Replikation trägt keinen initialen Snapshot-Export dieses Wegs — eine frühere Zeile bliebe über `GET /changes` unsichtbar, real geprüft); danach liefert `GET /changes?source=demo-source` real die Zeile, ohne dass ein Nutzer manuell SQL oder `make schema-rollout` aufruft. Idempotent: ein zweiter `example-demo-up`-Lauf überspringt den Rollout-Schritt gegen ein bereits migriertes Ziel über den eigenen, aufruferseitigen Existenz-Check von `examples/bootstrap.sh`; `make schema-rollout` selbst ist seit `schema-rollout-zentrale-idempotenz-wache` ebenfalls idempotent gegen die sieben bekannten Fremdobjekte (siehe `make schema-rollout`-Zeile oben) — der Aufrufer-Check bleibt trotzdem bestehen. Fügt die Demo-Zeile nur ein, wenn `public.orders` noch leer ist. `examples/.env` (committet, keine Vorlage, `ADR-0098` Festlegung 3) ist die gemeinsame Umgebungsdatei-Quelle für den Feed-Container (`env_file:`) und jedes `make example-run-*`-Ziel (`--env-file`). Braucht DB-Zugang und das benannte Docker-Netzwerk, deshalb Werkzeug statt Gate | kein Gate, [`ADR-0098`](../docs/plan/adr/0098-beispiel-clients-start-ueber-make-dockerfile.md) · seit slice-beispiele-compose-bootstrap |
| `make sdk-pack-csharp` | baut, testet und paketiert das C#-SDK-Package `PgChangeFeed.Client` (Bau-Kontext `sdks/csharp/`, gepinntes `mcr.microsoft.com/dotnet/sdk`-Image, Stufen `build`→`pack`→`pack-export`): `dotnet test` gegen alle vier Testflächen (HTTP, gRPC, SSE, NATS-Vollinhalt — dasselbe Testprojekt `PgChangeFeed.Client.Tests`) läuft vor `dotnet pack -c Release`; ein roter Test bricht den `docker build` mit Exit != 0 ab, bevor `pack` je erreicht wird (real geprüft: eine mutierte Testerwartung bricht den Bau ab, ohne Artefakt). Der Aufruf trägt `--build-context proto=proto` wie der gRPC-Bau der Fläche (`ADR-0090` Festlegung 2) — ohne ihn bricht der Bau an der `COPY --from=proto`-Zeile ab. Die Export-Stufe `pack-export` gibt das erzeugte `.nupkg` als `tar`-Stream über stdout aus; `tools/harness/sdk-pack-csharp.sh` extrahiert host-seitig über `docker run --rm --network none <image> \| tar -x -C sdks/csharp/dist/` (analog `make proto-generate`, `AGENTS.md` §3.9 Pipe-Disziplin) — real erzeugt: `sdks/csharp/dist/PgChangeFeed.Client.0.2.1.nupkg`. `sdks/csharp/dist/` ist `.gitignore`t. Braucht Netz (NuGet-Restore), deshalb Werkzeug statt Gate — dieselbe Begründung wie `make examples-csharp` | kein Gate, [`ADR-0106`](../docs/plan/adr/0106-csharp-nuget-erstes-sdk-package.md) Festlegung 4 · seit slice-sdk-csharp-pack-werkzeug |
| `make sdk-pack-python` | baut, testet und paketiert das Python-SDK-Package `pgchangefeed` (Bau-Kontext `sdks/python/`, gepinntes `python:3.14-slim`-Image, Stufen `build`→`pack`→`pack-export`): `pytest` läuft vor `uv build --no-sources`; ein roter Test bricht den `docker build` mit Exit != 0 ab, bevor `pack` je erreicht wird. Der Aufruf trägt `--build-context proto=proto` wie der gRPC-Bau der Fläche (`ADR-0090` Festlegung 2, seit `slice-sdk-python-grpc-client-flaeche`) — ohne ihn bricht der Bau an der `COPY --from=proto`-Zeile ab; die im Bau erzeugten gRPC-Stubs liegen als Unterpackage im Wheel, nicht committet. `uv` selbst wird nicht per `pip install` bezogen, sondern per digest-gepinntem Multi-Stage-`COPY` aus Astrals eigenem Werkzeug-Image (`ghcr.io/astral-sh/uv:0.12.17`, Digest real gegen die Registry verifiziert) — das bestehende `setuptools.build_meta`-Backend in `pyproject.toml` bleibt unverändert (`ADR-0108` §Entscheidung Festlegung 1/2/3). Die Export-Stufe `pack-export` gibt das erzeugte Artefakt-Paar (`.whl` **und** `.tar.gz`) als `tar`-Stream über stdout aus; `tools/harness/sdk-pack-python.sh` extrahiert host-seitig über `docker run --rm --network none <image> \| tar -x -C sdks/python/dist/` (analog `make sdk-pack-csharp`, `AGENTS.md` §3.9 Pipe-Disziplin) — real erzeugt: `sdks/python/dist/pgchangefeed-0.2.1-py3-none-any.whl`, `sdks/python/dist/pgchangefeed-0.2.1.tar.gz`. `sdks/python/dist/` ist `.gitignore`t. Braucht Netz (PyPI-Paketbezug für Test-Abhängigkeiten), deshalb Werkzeug statt Gate — dieselbe Begründung wie `make sdk-pack-csharp` | kein Gate, [`ADR-0107`](../docs/plan/adr/0107-python-pypi-zweites-sdk-package.md) Festlegung 5, [`ADR-0108`](../docs/plan/adr/0108-python-sdk-uv-statt-build-twine.md) §Entscheidung Festlegung 1/3, [`ADR-0110`](../docs/plan/adr/0110-python-sdk-umfang-erweitert-vollmatrix.md) · seit slice-sdk-python-pack-werkzeug |
| `make sdk-pack-kotlin` | baut, testet und paketiert das Kotlin-SDK-Package `pgchangefeed-kotlin` (Bau-Kontext `sdks/kotlin/`, gepinntes `eclipse-temurin:21-jdk`-Image, Stufen `build`→`pack`→`pack-export`): `./gradlew test` gegen alle vier Testflächen (HTTP, gRPC, SSE, NATS-Vollinhalt) läuft vor `./gradlew build`; ein roter Test bricht den `docker build` mit Exit != 0 ab, bevor `pack` je erreicht wird. Der Aufruf trägt `--build-context proto=proto` wie der gRPC-Bau der Fläche (`ADR-0090` Festlegung 2) — ohne ihn bricht der Bau an der `COPY --from=proto`-Zeile ab. Die `pack`-Stufe kopiert lediglich den bereits von `./gradlew build` erzeugten Standard-`jar`-Task-Output (kein zusätzlicher Gradle-Aufruf) — real recherchiert (`docs.github.com/…/publishing-java-packages-with-gradle`): GitHub Packages verlangt kein Sources-/Javadoc-Jar neben dem Haupt-Jar, ein einzelnes Jar genügt. Die Export-Stufe `pack-export` gibt das erzeugte `.jar` als `tar`-Stream über stdout aus; `tools/harness/sdk-pack-kotlin.sh` extrahiert host-seitig über `docker run --rm --network none <image> \| tar -x -C sdks/kotlin/dist/` (analog `make sdk-pack-csharp`/`make sdk-pack-python`, `AGENTS.md` §3.9 Pipe-Disziplin) — real erzeugt: `sdks/kotlin/dist/pgchangefeed-kotlin-0.2.1.jar`. `sdks/kotlin/dist/` ist `.gitignore`t. Braucht Netz (Maven-Central-/Gradle-Plugin-Portal-Paketbezug), deshalb Werkzeug statt Gate — dieselbe Begründung wie `make sdk-pack-csharp`/`make sdk-pack-python` | kein Gate, [`ADR-0109`](../docs/plan/adr/0109-kotlin-github-packages-drittes-sdk-package.md) Festlegung 5 · seit slice-sdk-kotlin-pack-werkzeug |
| `make test-sdk-kotlin-integration` | Realserver-Integrationstest der Kotlin-SDK-Zustellweg-Flächen (slice-sdk-kotlin-reale2e, Mechanik-Klasse [`ADR-0110`](../docs/plan/adr/0110-python-sdk-umfang-erweitert-vollmatrix.md) §Entscheidung Festlegung 2, gespiegelt vom C#-Werkzeug): `tools/harness/run-sdk-kotlin-integration-tests.sh` fährt die `compose.yaml`-Umgebung hoch (Bring-up-Phase im Python-Runner-Muster), baut die neue `integration`-Docker-Stufe des Kotlin-SDK (`sdks/kotlin/Dockerfile`, zwingend mit dem benannten Bau-Kontext `proto`, Pins aus der bestehenden `build`-Stufe wiederverwendet) und startet je Fläche eine Phase (`./gradlew integrationTest --tests` je Testklasse, Testklasse als Umgebungsvariable mit `:?`-Guard gegen blanken Aufruf; der Gradle-Task hängt bewusst nicht an `check` — `make sdk-pack-kotlin` bleibt netzlos grün, real gemessen) im selben Docker-Netz wie den Feed-Container — der Prüfling ist die kompilierte Client-Assembly (`src/integrationTest/kotlin` importiert `io.github.pt9912.pgchangefeed`, kein Wegwerf-Duplikat-Client). Der Lauf belegt: gRPC-/SSE-/NATS-Client empfangen real eine committete Änderung (`change_id` je unabhängig über `cdc.changes` lesbar), die HTTP-Fläche registriert real einen Consumer (unabhängig über `cdc.consumer` lesbar) und listet Tabellen; ein Aufruf ohne Token endet mit gRPC-Status `Unauthenticated`, HTTP-Status `401` bzw. der NATS-Verbindungsablehnung. Der Runner schreibt den Kotlin-Abschnitt des Abdeckungs-Trägers [`docs/user/sdk-e2e-abdeckung.md`](../docs/user/sdk-e2e-abdeckung.md) aus derselben Messung (marker-gegrenzt, idempotent, fremde Abschnitte erhalten). Setzt ein geladenes `:dev`-Image voraus (`make image` vorher — [`ADR-0044`](../docs/plan/adr/0044-image-beleg-semantik.md)); trägt den Python-HTTP-Abschnitt im Abdeckungs-Träger seit slice-sdk-python-http-reale2e | kein Gate, slice-sdk-kotlin-reale2e · seit slice-sdk-kotlin-reale2e |
| `make test-sdk-csharp-integration` | Realserver-Integrationstest der C#-SDK-Zustellweg-Flächen (slice-sdk-csharp-reale2e, Mechanik-Klasse [`ADR-0110`](../docs/plan/adr/0110-python-sdk-umfang-erweitert-vollmatrix.md) §Entscheidung Festlegung 2, gespiegelt vom Python-Werkzeug): `tools/harness/run-sdk-csharp-integration-tests.sh` fährt die `compose.yaml`-Umgebung hoch (Bring-up-Phase im Python-Runner-Muster), baut die neue `integration`-Docker-Stufe des C#-SDK (`sdks/csharp/Dockerfile`, zwingend mit dem benannten Bau-Kontext `proto`, Pins aus der bestehenden `build`-Stufe wiederverwendet) und startet je Fläche eine Phase (`dotnet test --filter` je Testklasse, Testklasse als Umgebungsvariable mit `:?`-Guard gegen blanken Aufruf) im selben Docker-Netz wie den Feed-Container — der Prüfling ist die kompilierte Client-Assembly (`PgChangeFeed.Client.Integration` referenziert `PgChangeFeed.Client`, kein Wegwerf-Duplikat-Client). Der Lauf belegt: gRPC-/SSE-/NATS-Client empfangen real eine committete Änderung (`change_id` je unabhängig über `cdc.changes` lesbar), die HTTP-Fläche registriert real einen Consumer (unabhängig über `cdc.consumer` lesbar) und listet Tabellen; ein Aufruf ohne Token endet mit gRPC-Status `Unauthenticated`, HTTP-Status `401` bzw. der NATS-Verbindungsablehnung. Der Runner schreibt den C#-Abschnitt des Abdeckungs-Trägers [`docs/user/sdk-e2e-abdeckung.md`](../docs/user/sdk-e2e-abdeckung.md) aus derselben Messung (marker-gegrenzt, idempotent); `trace.coverage`-Eintrag (Label `SDK-E2E`) in `.d-check.yml` im selben Zug. Setzt ein geladenes `:dev`-Image voraus (`make image` vorher — [`ADR-0044`](../docs/plan/adr/0044-image-beleg-semantik.md)); trägt die Kotlin- und Python-HTTP-Abschnitte im Abdeckungs-Träger seit slice-sdk-kotlin-reale2e bzw. slice-sdk-python-http-reale2e | kein Gate, slice-sdk-csharp-reale2e · seit slice-sdk-csharp-reale2e |
| `make test-sdk-python-integration` | Realserver-Integrationstest der Python-SDK-Zustellweg-Flächen ([`ADR-0110`](../docs/plan/adr/0110-python-sdk-umfang-erweitert-vollmatrix.md) §Entscheidung Festlegung 2/Folgepflicht 1, [`LH-FA-SST-008`](../spec/lastenheft.md)): `tools/harness/run-sdk-python-integration-tests.sh` fährt die `compose.yaml`-Umgebung hoch (PostgreSQL/NATS/Feed-Container, Schema-Rollout über d-migrate, Vorbedingungen der Aktivierung — Bring-up-Phase im Muster `make test-integration`, bewusst schmaler: kein Server-E2E-Beleg, der bleibt Eigentum des bestehenden Runners), baut die neue `integration`-Docker-Stufe des Python-SDK (`sdks/python/Dockerfile`, zwingend mit dem benannten Bau-Kontext `proto`, `ADR-0090` Festlegung 2 — die gRPC-Stubs entstehen im Bau aus der `.proto` und sind nicht committet, dasselbe Muster wie C#/Kotlin) und startet sie im selben Docker-Netz wie den Feed-Container — der Prüfling ist das SDK selbst (je Phase eine Fläche: `pgchangefeed.grpc_client`, `pgchangefeed.sse_client`, `pgchangefeed.nats_stream_client`, `pgchangefeed.http_client` — die HTTP-Fläche seit slice-sdk-python-http-reale2e nachgezogen, keine Wegwerf-Duplikat-Clients; jede Phase traegt ihre Testdatei explizit als Umgebungsvariable `PGCHANGEFEED_TEST_FILE`, deren Guard blanken Aufruf laut scheitern laesst). Der Lauf belegt: die Stream-Clients empfangen real eine zuvor per `psql` committete Änderung über ihre Streams (Tabelle, Operation, Sentinel am Wire; `change_id` unabhängig über `cdc.changes` lesbar), die HTTP-Fläche registriert real einen Consumer (unabhängig über `cdc.consumer` lesbar) und listet Tabellen, und ein Aufruf ohne Token endet mit gRPC-Status `Unauthenticated`, HTTP-Status `401` bzw. der laut ablehnenden NATS-Verbindungsablehnung („Authorization Violation"). Der Runner schreibt den Python-Abschnitt des Abdeckungs-Trägers [`docs/user/sdk-e2e-abdeckung.md`](../docs/user/sdk-e2e-abdeckung.md) (marker-gegrenzt, idempotent, fremde Abschnitte erhalten). Setzt ein geladenes `:dev`-Image voraus (`make image` vorher — `compose.yaml` trägt keinen `build:`-Block, [`ADR-0044`](../docs/plan/adr/0044-image-beleg-semantik.md)) | kein Gate, [`ADR-0110`](../docs/plan/adr/0110-python-sdk-umfang-erweitert-vollmatrix.md) §Entscheidung Festlegung 2 · seit slice-sdk-python-grpc-client-flaeche |
| `make <mover>` | bewegt <…>, prüft nichts | kein Gate |
| `make <messung>` | misst <…> gegen <Schwelle> | kein Gate, ADR-<NNNN> |
| `make <vorschau>` | sagt, was <schreibender Lauf> täte; Ausgänge und Sperren in der verlinkten Datei | kein Gate |

**Aktueller Lauf-Status:** [![ci](https://github.com/pt9912/pg-change-feed/actions/workflows/ci.yml/badge.svg)](https://github.com/pt9912/pg-change-feed/actions/workflows/ci.yml)
bzw. lokal `make help` / `make gates`. Der Workflow
[`.github/workflows/ci.yml`](../.github/workflows/ci.yml) automatisiert
diesen Gate-Lauf (plus `make test`) auf jeden Pull Request und Push — kein
neues Gate, nur die Automatisierung des bestehenden
([`ADR-0051`](../docs/plan/adr/0051-cicd-pipeline-github-actions.md)).
Unmittelbar nach dem Checkout, vor dem `Gates`-Schritt, prüft ein eigener
Workflow-Schritt `uname -s`/`go env GOOS` gegen `Linux`/`linux` mit
sichtbarem Fehlschlag bei Abweichung — der sichtbare Log-Beleg für
[`LH-QA-POR-002`](../spec/lastenheft.md)
([`ADR-0058`](../docs/plan/adr/0058-testansatz-fuenf-luecken.md)
Entscheidung 5, kein Gate).
**Rote Gates:** Begründung im verlinkten `CO-<NNN>` (siehe Bindung-Spalte), Modul 7.

<!-- Domänenspezifische Gates ergänzen, je nach Repo-Klasse: -->

## Traceability rules

- PRs/Commits **müssen** mindestens eine `<LH-*>` oder `ADR-*`-ID nennen.
- Mechanisch getragen (Standing-Gate, [`ADR-0045`](../docs/plan/adr/0045-commit-traceability-standing-gate.md)): `make commit-traceability` prüft je Message der letzten 5 Commits (`RANGE=base..head` überschreibt) beide Grenzen — positive Hälfte via d-check Modul `commits` (Befund `commit-untraceable`), Betreff-Grenze via `tools/harness/commit-traceability.sh` · seit slice-006 (ausgelöst durch das dritte Auftreten der Verstoß-Klasse, review-slice-006 F-2).
- Optional, **nicht-durchsetzend** ([`ADR-0062`](../docs/plan/adr/0062-lokaler-commit-msg-hook-ergaenzt-standing-gate.md), Zusage einseitig nach [`ADR-0069`](../docs/plan/adr/0069-commit-msg-hook-einseitige-zusage.md)): der lokale `commit-msg`-Hook unter `.githooks/commit-msg` meldet vorab in den Klassen, in denen er liest — er fängt **nicht** jeden Verstoß, den `make commit-traceability` fängt, und weist keinen Commit zurück, den das Gate zulässt. Aktivierung ist ein einmaliger, lokaler Schritt — `git config core.hooksPath .githooks` (`core.hooksPath` ist lokale Konfiguration, sie reist nicht mit dem Klon; `git commit --no-verify` umgeht den Hook). Er ersetzt `make commit-traceability` nicht: er läuft ausschließlich lokal vor `git commit` und ist kein Gate-Ziel · seit slice-073.
- Neue oder geänderte Anforderungen brauchen einen Beleg: Test, Gate, Demo oder ADR.
- Neue ADRs müssen im ADR-Index ergänzt werden.
- Änderungen an Planning-Dokumenten müssen die Lifecycle-Regeln beachten (open → next → in-progress → done; reine `git mv`-Commits siehe AGENTS.md §3.3).

## Safety and scope boundaries

<!--
Repo-spezifisch formulieren. Beispiele:

Für ein Referenz-Repo:
- Dies ist kein produktiver Service.
- Externer Cloud-Zugriff darf nicht für lokale Demo-Abnahme vorausgesetzt werden.
- Determinismus und Replayability sind Kernverträge.

Für ein Safety/Control-Repo:
- Markt-/Optimierungs-Output muss durch Statemachine, Constraint-Limiter, Ramp-Limiter fließen.
- Software-Stop ersetzt keine Hardware-Sicherheitsfunktionen.
- Produktion-Profile müssen fail-closed sein.

Für ein Policy/Compliance-Repo:
- Dieses Werkzeug ist keine Rechts-/Steuer-/Fachberatung.
- KI-Funktionen liefern Vorschläge, keine verbindlichen Entscheidungen.
-->

- <…>
- <…>

## Minimal agent workflow

1. Diese Datei lesen.
2. Relevante kanonische Quelle lesen (Source Precedence beachten).
3. Betroffene Requirement-/ADR-IDs identifizieren.
4. Kleinste sinnvolle Änderung planen.
5. Engsten nützlichen Sensor laufen lassen.
6. Repo-weiten Gate-Lauf vor Handoff (`make gates`) — Exit-Code direkt prüfen, nie durch eine Pipe/einen Wrapper hindurch (`AGENTS.md` §3.9).
7. Doku/Indizes aktualisieren, falls ein öffentlicher Vertrag berührt.
8. Ausgeführte Sensors und verbleibende Risiken berichten — keine Erfolgsmeldung ohne Gate-Ausführung.

Dieser Workflow deckt ausschließlich die Implementer-Rolle ab. Schritt 8
ist der Rollenwechsel, kein Abschluss: Bericht → Handoff an Reviewer
(`.harness/skills/reviewer.md`, siehe `harness/README.md` §Guides) →
Verifier. Kein Self-Review — anderer Kontext findet andere Findings,
derselbe Kontext dieselben blinden Flecken (Baseline-Regelwerk
`modul-08-agentenrollen.md`).

## Leseordnung

Regeln dieser Sektion: Baseline-Regelwerk `grundlagen-harness-dateien.md`
§harness/README.md als Einstiegspunkt — die Menschen-Hälfte des Einstiegs:
drei bis fünf **geordnete** Zeiger, was ein neuer Mensch zuerst liest und was
bei Bedarf; eine Leseordnung, die alles nennt, ist keine.

1. <zuerst — z. B. `AGENTS.md` §Hard Rules>
2. <dann — z. B. `spec/lastenheft.md`>
3. <bei Bedarf — z. B. `harness/conventions.md`>
