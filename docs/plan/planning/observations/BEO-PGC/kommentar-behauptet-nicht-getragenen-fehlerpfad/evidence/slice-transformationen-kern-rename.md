**Vorgang:** slice-transformationen-kern-rename (Review-Fund F-1 HIGH, Verifikations-Fund V-1 LOW)

**Fund:** Der Doc-Kommentar von `model.BuildRowImage` (`internal/domain/model/rowimage.go`) sagte, eine nicht anwendbare Regel „wirkt hier nicht“, und nannte im selben Satz, sie ergäbe zwei gleichnamige Schlüssel. Für den Fall „Zielname kollidiert“ trug der Code die Zusage nicht: die Regel wirkte, das Bild trug zwei gleichnamige Schlüssel ohne Fehler (Probe des Reviewers: `{"id":"1","customer_name":"Ada","customer_name":"Kunde"}`). Der künftige Backfill-Aufrufer liest diesen Kommentar als Vertrag der Vorbedingung. Die Fixrunde trug die Zusage im Code (Kollisionsprüfung an der Schreibstelle, `ErrTransformationTargetCollides`, Einordnung im Mapper über `imageError`) statt den Satz umzuformulieren; damit entstand ein zweiter Ort, an dem `ErrTransformationNotApplicable` gemeldet wird, **nach** dem Vorrücken der Sequenz. Der Doc-Kommentar von `Assembler.change` sagte weiter, eine nicht anwendbare Regel ende „ohne dass … die Sequenz vorrückt“ — für die Kollision zweier Regeln stimmte das nicht (Verifikation V-1, LOW, im Doc-Kommentar nachgezogen). Der Kommentar am Sentinel `ErrTransformationNotApplicable`, den der Plan an dieser Klasse ausrichtete, trug fehlerfrei.

**Form:** dieselbe Klasse wie die drei Vorgänger (Kommentar sagt einen Fehlerpfad zu, den der Code nicht trägt), mit einer neuen Ausprägung: die **Behebung** eines solchen Funds ändert den Code statt des Texts und verschiebt dadurch die Aussage der **Nachbar-Kommentare** desselben Pfads. Gefunden haben beide Funde Leser — der Reviewer durch eine Wegwerf-Probe, der Verifier durch Nachlesen der Reihenfolge im Code —, kein Sensor; beide vor dem Merge.

Quelle: `docs/reviews/review-slice-transformationen-kern-rename.md` (F-1) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-transformationen-kern-rename.md` (V-1, §5 Zeile F-1). <!-- d-check:status-provenance -->
