**Vorgang:** slice-transformationen-start-reihenfolge (Review F-2, MEDIUM; Verifikation V-3, INFO)

**Fund:** Der Godoc von `runStreamAfterAdministrationPass` (`internal/bootstrap/wiring.go`) sagte in der ersten Fassung, ein beim Prozessstart `pending` stehender Antrag sei „vermerkt und in der `Assembler`-Bindung nachgetragen, bevor der Stream die erste Transaktion assembliert; die Ordnung gilt für jede Antragsart“, ohne Bedingung. Der Rumpf trägt das nur für den Durchlauf, der die Queue liest: liest `ListPending` nicht (Warnung, `return`), bleibt jeder Antrag `pending` und der Stream läuft mit dem bisherigen Regelstand; scheitert `MarkApplied`, steht die Wirkung in der Bindung, und der Antrag bleibt `pending`, bis die Goroutine ihn erneut verarbeitet. Die Einschränkung stand zwei Sätze später ohne Bezug auf die Zusage. Für die Abhilfe nach einer nicht anwendbaren Regel heißt das: schlägt die Lesung im Vorlauf einmal fehl, endet der Prozess wieder mit der Klasse `schema`. Die Fixrunde (`eb4c2b1c`) nennt Bedingung, Lesefehler-Pfad, Vermerk-Pfad und die fehlende Frist im Godoc; die Verifikation las den Wortlaut gegen `processAdministrationRequests` und bestätigte die Behebung. Rest (Verifikation V-3, INFO): „für jede Antragsart“ meint für `backfill` „angenommen“ (Run-Zeile `queued`, Antrag `applied`), eine Bindung wird nicht berührt, und ein `backfill`-Antrag einer anderen Quelle bleibt im Vorlauf `pending`; der Godoc ist dazu nicht geändert, der Plan nennt es in §3.

**Form (Ausprägung):** dieselbe Klasse (eine Zusage über einen Pfad, den der Code nicht überall trägt), hier in der **Bedingung**: der Kommentar sagt eine Ordnung zu, die vom Erfolg einer Lesung abhängt, und nennt diese Abhängigkeit nicht bei der Zusage. Der Reviewer stufte MEDIUM (der Skill führt die Zusage-Unterklasse unter HIGH; Begründung: der Code trägt das beschlossene Verhalten, best effort, und die Einschränkung stand im selben Godoc — die Form der Zusage war der Fehler). Vor dem Merge vom Reviewer gefunden, die Probe „den zugesagten Pfad im Code nachfahren“ hat gegriffen; Ausgang des Eintrags unverändert **verkörpert**.

Quelle: `docs/reviews/review-slice-transformationen-start-reihenfolge.md` (F-2) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-transformationen-start-reihenfolge.md` (§5 Zeile F-2, V-3). <!-- d-check:status-provenance -->
