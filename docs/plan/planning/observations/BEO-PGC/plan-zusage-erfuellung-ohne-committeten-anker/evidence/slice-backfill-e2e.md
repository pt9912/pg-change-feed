**Vorgang:** slice-backfill-e2e (Verifikation V-1 und V-2, Planner-Closure)

**Fund:** Zwei DoD-Zusagen des Plans nannten keinen committeten Träger ihrer Erfüllung. *V-1 (LOW):* §2 Punkt 1 sagt „der Architect bestätigt die Ortswahl im Review oder verlangt zusätzlich den Tier-Beleg“ für den Ort der Replay-Invariante (E2E statt `make test-replication`, `ADR-0111` nennt beide Orte); weder der Review-Report noch die zwei Architect-Verdikte der Welle nennen die Ortswahl (`grep -i` in den drei Dateien: nur die gedruckte Lauf-Zeile im Review). Der Beleg selbst ist erbracht (beide CI-Legs, `TestE2EBackfillReplayInvariant`). *V-2 (LOW):* §2 Punkt 2 sagt „jedes dieser Kriterien trägt je eine Mutation im Bericht“; der Bericht des Implementers liegt nicht im Repo, ein committetes Artefakt nennt die Mutationen der Negative-Phase (`docker kill`, `queued`-Aufnahme) nicht, und der Verifier fuhr sie nicht (Lauf-Kosten je rund 5 Minuten). Die Closure beantwortet beide im Plan: V-1 als benannte Entscheidung des Planners mit Begründung (§3), V-2 als im Repo nicht belegt (§7).

Quelle: `docs/reviews/verifikation-slice-backfill-e2e.md` (V-1, V-2, §12). <!-- d-check:status-provenance -->
