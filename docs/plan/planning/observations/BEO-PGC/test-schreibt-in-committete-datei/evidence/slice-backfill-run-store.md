**Vorgang:** slice-backfill-run-store (Review-Prüfungen zum Rollout-Beleg, Verifikation §1, V-6; Closure-Messung)

**Fund:** `make test-store` und `make test-replication` überschreiben über `tools/schema/apply-rollout.sh` die `target`-Zeile der committeten Datei `tools/schema/plan.yaml` (Tier-Ziel statt des committeten Compose-Werts `cdc-test-postgres`); der Reviewer nahm die Datei nach jeder Mutation und jedem Tier-Lauf per `git checkout` zurück, der Verifier nach jedem Lauf (V-6, INFO), und die Closure-Messung der Sensor-Zahlen (`make test-store` gefolgt von `make test-replication`) hinterließ dieselbe Änderung im Arbeitsbaum (`git status --short`: ` M tools/schema/plan.yaml`), bis sie zurückgenommen war. Der Slice selbst committete `plan.yaml` und `down.sql` als Ergebnis eines Rollouts gegen eine leere DB; der Verifier belegte, dass `down.sql` byte-gleich und `plan.yaml` bis auf die `target`-Zeile gleich dem frischen Rollout ist. Derselbe Pfad wie bei den Vorbelegen, hier in den Läufen dreier Rollen desselben Vorgangs (Reviewer, Verifier, Planner-Closure); die Läufe der zwei DB-Tiers besitzen weiterhin keine Wiederherstellung.

Quelle: `docs/reviews/verifikation-slice-backfill-run-store.md` (§1 Tabelle, §3.3, V-6) <!-- d-check:status-provenance -->
· `docs/reviews/review-slice-backfill-run-store.md` (Abschnitt „Rollout-Belege", Mutationen: „`tools/schema/plan.yaml` als bekannter Tier-Nebeneffekt jeweils zurückgenommen"). <!-- d-check:status-provenance -->
