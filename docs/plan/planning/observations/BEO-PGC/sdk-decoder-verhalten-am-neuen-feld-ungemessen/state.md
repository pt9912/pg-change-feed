Zustand: offen — Ausgang: **weiter offen**, adressiert bei der Closure von
`welle-backfill-bestand` (Architect im Lese-Schritt: ein Realserver-Beleg der HTTP-Fläche
als Folge-Slice, oder Streichung mit Begründung). Belegt ist das Lesen von `origin` in den
drei Modellen am Unit-Tier (Fixtures in der Antwortform des Handlers, sieben
Eingabeseiten-Mutationen rot); ungemessen bleiben der Lauf gegen einen Server-Container und
das Verhalten der veröffentlichten Packages `0.1.0` gegenüber einem unbekannten Feld — der
Bestand der `make test-sdk-*-integration`-Läufe ruft `GET /changes` nicht auf.
Zähler (abgeleitet): 2× (evidence/slice-backfill-change-origin.md,
evidence/slice-backfill-sdk-origin.md).
