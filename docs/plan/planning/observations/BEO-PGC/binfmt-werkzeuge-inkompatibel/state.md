Zustand: offen — Ausgang: **weiter offen** (unter der 3×-Schärfungsschwelle).
Kein Sensor kann das fangen (System-/Umgebungszustand außerhalb des Repos);
die tragende Abhilfe bleibt Disziplin: `tonistiigi/binfmt` und
`multiarch/qemu-user-static` nicht nacheinander auf demselben Colima-Setup
ausführen, `colima restart` als Wiederherstellungsschritt kennen. Zähler
(abgeleitet): 1× (evidence/slice-release-multi-arch-image.md).
