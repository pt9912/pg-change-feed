**Vorgang:** slice-075
**Fund:** Der `open→next`-Übergang des Slice brach **drei** Inbound-Links, die
den Slice mit festem Lifecycle-Verzeichnis adressierten — zwei in
`BEO-PGC/laufzeitzustand-ohne-dauerhaften-traeger/` (`observation.md`,
`state.md`), einer im Architect-Verdikt zur
Spaltenausschluss-Dauerhaftigkeit.
`make gates` lief real rot (Exit 2, `docs-check`, drei `target-missing`).
Behoben durch Umstellung auf die Kennungs-Zitierung (`slice-075`). Drittes,
unabhängiges Auftreten — Schwelle erreicht.
