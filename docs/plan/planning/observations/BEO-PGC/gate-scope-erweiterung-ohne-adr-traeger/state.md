Zustand: offen — Ausgang noch nicht zugewiesen. Mit `ADR-0068` steht die
tragende Regel (Verfeinerung vs. Erweiterung, ausdrücklich über `layers`,
`edges` **oder** `composition_root`) bereits — ob daraus zusätzlich ein
Reviewer-Skill-Punkt gehört, entscheidet der Lese-Schritt bei 3×; bei 2×
noch nicht fällig. Zähler (abgeleitet): 2× (evidence/slice-071.md,
evidence/adr-0099-slice-welle-review-regel.md).
