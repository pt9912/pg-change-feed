Zustand: **verkörpert** — Ausgang: **verkörpert**.

Tragende, unabhängige Linie (mechanisch): `forbid-pattern` auf der fünften
`structure`-Regel in `.d-check.yml` — Dateiklasse
`docs/plan/planning/done/slice-*.md`, Abschnitt `## 7. Closure-Notiz`. Ein
stehengebliebener Ausfüll-Hinweis färbt `make docs-check` als
`section-forbidden` rot. Erste, nicht tragende Linie: die datei-skopierte
Selbstprüfung im Planner-Closure-Schritt
(`.claude/commands/implement-slice.md` Schritt 24) auf dem **rohen** Text.
Der Architect-Verdikt zum Vorlagenrest in der Closure-Notiz trägt die Details.

Herkunfts-Anker `seit welle-19`: Die Klasse erreicht die Schwelle mit den
Slices dieser Welle (`slice-069`…`slice-072`); der Anker löst über
`done/welle-19-results.md` auf, das die Klasse in §Beobachtungs-Register
benennt und den Architect-Zug des Lese-Schritts dorthin übergibt — der Lauf
selbst konnte ihn nicht dispatchen.

Vierter Beleg, im Architect-Zug gefunden: `slice-069` behielt nur den
kursiven Hinweis (Anker-Zeile und `Auslöser:`-Zeile waren entfernt) — genau
die Form, die die Anker-Paarung nicht sieht, weil das Feld `liegt in` fehlt.
Der Beleg ist gezählt (`evidence/slice-069.md`), der Rest entfernt.

Grenze des Sensors, benannt: Die `structure`-Bedingungen lesen den
**bereinigten** Abschnittstext, in dem Inline-Code-Spans geleert sind. Ein
stehengebliebener Zielort-Platzhalter (`<…>` im Backtick-Zielort der
Anker-Zeile) ist damit unsichtbar; mechanisch greifbar ist der Hinweis-Text,
der in allen vier Belegen mitging. Die rohe `grep`-Selbstprüfung deckt diese
Grenze ab.

Kein Reviewer-HIGH-Punkt — geprüft und verworfen: Der beobachtete Marker ist
jetzt mechanisch getragen (unabhängige Linie), der Closure-Note-Reviewer
schließt eine Dopplung des Struktur-Gates ausdrücklich aus, und der Fall ist
ein Form-Rest, kein Satz-Subjekt-Urteil.

Keine Bestands-Ausnahme: die Regel ist gegen den ganzen
`done/slice-*.md`-Korpus grün — kein Alt-Vorgang trägt den Hinweis noch.

Zähler (abgeleitet): 4× (evidence/slice-069.md, evidence/slice-070.md,
evidence/slice-071.md, evidence/slice-072.md).
