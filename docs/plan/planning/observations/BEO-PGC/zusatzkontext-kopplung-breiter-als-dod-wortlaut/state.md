Stand: offen (2×, unter der 3×-Schwelle — kein Ausgang fällig).

Zähler (abgeleitet): **2×** (evidence/slice-102.md, evidence/slice-103.md).
Beide Vorgänge treffen dieselbe strukturelle Ursache (eine gemeinsame
`build`-Stufe koppelt den benannten Zusatzkontext an alle vier Programme
einer Sprache statt nur an `grpc`), jeweils in einer anderen Sprache
(C#/Kotlin). Kein Ausgang zugewiesen, solange die Schwelle nicht erreicht
ist.
