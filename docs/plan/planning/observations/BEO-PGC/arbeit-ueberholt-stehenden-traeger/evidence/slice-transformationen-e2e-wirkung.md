**Vorgang:** slice-transformationen-e2e-wirkung (Suchlauf des Implementers, Plan §3 Zeile „Waisen-Aussage im RTM-Träger“; Verifikation V-3, LOW; Review F-6, LOW)

**Fund:** Die bewegte Eigenschaft ist der Deckungsstand von `LH-FA-CFG-007` im RTM-Lauf (`make doc-trace`). Der E2E-Beleg der Phase „Backfill-Regelstand“ (`fc0b8d38`, Slice `slice-transformationen-backfill-pfad`) schrieb die erste Zeile für die Anforderung in `docs/user/e2e-abdeckung.md` und nahm sie damit aus den Waisen. Zwei Träger zogen das nicht nach: die Zeile `make doc-trace` in `harness/README.md` (Messung vom 2026-09-24 mit der Zahl zwei) und die Zeile des Slice `slice-transformationen-e2e-wirkung` in der Tabelle von `welle-transformationen`, die diesem Slice die Wirkung zuschrieb, die Anforderung aus den Waisen zu nehmen. Gemessen bei der Closure: `make doc-trace` am Parent-Stand `38c7b3bc` (aus `git archive` in einem Wegwerf-Verzeichnis): „80 Anforderung(en), 1 Waise(n).“, `LH-FA-CFG-007` mit Deckung `E2E`, `LH-FA-CFG-008` `WAISE`; am Arbeitsbaum dieselbe Zeile. Der Plan von `slice-transformationen-backfill-pfad` nennt „Waise“ nicht (`grep -n -i waise` ohne Treffer, gemessen bei der Closure): sein Suchlauf trug die Eigenschaft nicht. Den Fund machte der Suchlauf des Implementers dieses Slice (Plan §3, beide Stände gemessen); die Zahl in `harness/README.md` steht seither im Ist-Stand, den Zuschreibungs-Satz der Slice-Tabelle zieht die Closure nach (die Meldung im committeten Feld nannte zunächst keine Frist, Review F-6; die Fixrunde ergänzte Adresse und Frist).

**Form (Ausprägung):** derselbe Mechanismus wie im Erstauftreten (die Arbeit eines Slice überholt einen stehenden Träger, der außerhalb seines Diffs liegt), gefunden **nach dem Merge des bewegenden Slice** und vom Suchlauf des Folge-Slice statt vom Reviewer; daher Datei trotz Deckel. Die Regel `AGENTS.md` §3.13 hat gewirkt: der Fund kam aus der Handlung „Träger nach der bewegten Eigenschaft suchen“, nicht aus einem Sensor.

Quelle: `docs/reviews/verifikation-slice-transformationen-e2e-wirkung.md` (§8 V-3, §1 Zeile `make doc-trace` am Parent-Stand) <!-- d-check:status-provenance -->
· `docs/reviews/review-slice-transformationen-e2e-wirkung.md` (F-6). <!-- d-check:status-provenance -->
