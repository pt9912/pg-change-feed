**Vorgang:** slice-backfill-change-origin (Review-Fund F-4, Planner-Closure)

**Fund:** Die bewegte Eigenschaft war die Feldzahl der Antwort von `GET /changes` (zwölf Felder am Parent, dreizehn mit `origin`). Das committete Suchlauf-Feld des Plans suchte die Anzahl-Formulierung in `docs/user` und `spec` und die Feldvorbild-Kommentare in `*.go`, `*.proto` und `*.py`; die SDK-Träger in `*.kt` und `*.cs` fehlten — die Kotlin-KDoc `sdks/kotlin/pgchangefeed-kotlin/src/main/kotlin/io/github/pt9912/pgchangefeed/sse/model/Change.kt` („twelve" am Zeilenende, „fields" in der Folgezeile — eine Zeilen-Suche auf „twelve fields" träfe sie nicht) und zwei C#-Kommentare („eleven fields", am Parent bereits ungleich zu den gezählten zwölf Feldern). Der Reviewer fand sie über `git grep -n -i -E 'twelve|eleven' HEAD -- '*.kt' '*.cs'` (F-4, LOW). Fixrunde 2 erweiterte den Suchraum auf den ganzen Baum (Wortsuche, unabhängig vom Zeilenumbruch) und meldete die Träger als eigene §3-Zeile an `slice-backfill-sdk-origin`; die SDK-Dateien bleiben unberührt, weil sie zum Folge-Slice gehören. Zwei weitere Träger derselben bewegten Eigenschaft — die Zeile mit „Exit 8 auf vier Fremdobjekten" in `tools/harness/run-integration-tests.sh` (F-7) und die Feldvorbild-Aussage in `proto/cdc/stream/v1/changestream.proto` samt generierter Datei (F-10) — stehen als gemeldete Träger im Plan, weil ihre Änderung Zeilenanker der E2E-Abdeckung verschöbe bzw. `make proto-generate` verlangte; sie zählen im selben Vorgang mit, nicht einzeln. Fund vom Reviewer, nicht vom Suchlauf des Implementers; Anwendungs-Schärfung der verkörperten Regel `AGENTS.md` §3.13 (Suchraum nach Dateitypen, die die bewegte Eigenschaft tragen können, nicht nach den Dateitypen des eigenen Diffs).

Quelle: `docs/reviews/review-slice-backfill-change-origin.md` (F-4, F-7, F-10). <!-- d-check:status-provenance -->
