**Vorgang:** slice-backfill-spec-nachzug (Review-Fund F-2, Verifier-Beobachtung V-1, Planner-Closure)

**Fund:** Bewegte Eigenschaften: die zwei Warn-Spalten von `SPEC-029` sind `boolean NOT NULL DEFAULT false` (nie „leer", Zahl und Namen festgelegt) und `SPEC-019` führt fünf Antragsarten. Falsch wurden Sätze in offenen Folge-Plänen, außerhalb des Diffs: „leer, solange keine Auswertung sie setzt", „vier SQL-Funktionen", „vier Arten" (9 Zeilen in 5 Plan-Dateien, gleich am Parent `77c60bd1` und am Diff-Stand `88bee004`). Der committete Suchlauf des Implementers las `spec`, `docs/user`, `harness` und die READMEs, nicht `docs/plan/planning`; der Reviewer fand die Träger (F-2, LOW), die Fixrunde (`6d6ee7ea`) zog sie und ergänzte die Suchlauf-Zeile. Zweite Schicht derselben Lücke: das Muster der Fixrunde suchte die „leer"-Formen, nicht den **Hedge** („Warn-Spalte(n)", „Spalte(n)"), der die nun geschlossene offene Zahl trug — der Verifier maß ihn nach (V-1, INFO; er nannte 14 Zeilen in vier Dateien), der Planner-Zug `95bd14a8` zog 17 Zeilen in fünf Dateien auf „zwei Warn-Spalten" (die Tabellenzeile in `slice-backfill-run-store` und die zwei Zeilen in `slice-backfill-run-usecase` trägt dasselbe Muster zusätzlich; 17 am Stand vor dem Zug, 0 danach). Unter-Klasse „gefunden vom Reviewer bzw. Verifier, nicht vom Implementer-Suchlauf". Geschärfte Anwendung: ein Slice, der eine bis dahin offene Zahl oder Menge festlegt, sucht den **Hedge des offenen Punkts** („(n)", „offen", „noch nicht") über alle Plan-Träger, nicht nur den Vorzustand des Werts.
