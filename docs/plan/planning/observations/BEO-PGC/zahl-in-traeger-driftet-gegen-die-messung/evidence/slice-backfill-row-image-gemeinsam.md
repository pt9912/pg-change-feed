**Vorgang:** slice-backfill-row-image-gemeinsam

**Fund:** Zwei Zahl-Träger-Funde in einem Vorgang. (1) Review F-1 (HIGH): das committete §3.13-Suchlauf-Feld des Plans nannte „Parent: 17 Treffer" für `rowImage` in `*.go`, zerlegt in 5 + 4 + 6 + 1; die Summanden stimmten, die Summe nicht (`git grep -n 'rowImage' 43f394a1 -- '*.go'` liefert 16). Der als Parent-Befehl genannte Aufruf trug außerdem `HEAD` statt des Parent-Commits und reproduzierte am Kopf weder 17 noch 16 (15). Die Summe war eine abgeleitete Zahl, die als gemessene stand; der Parent-Bezug war zeitrelativ. Die Fixrunde (`cd046787`) nennt 16 und 15 mit dem Parent-Commit als Stand. (2) Review F-4 (INFO): die Leistungs-Zahlen des Implementers (43 → 44 Allokationen, 1642 → 1739 B) standen in keinem committeten Träger; der Review maß mit anderem Aufbau (6 Spalten, eine ausgeschlossen, beide Bilder je Änderung) 64 → 65 allocs/op und 1313 → 1409 B/op. Der Plan trägt nach der Fixrunde die Wirkung („eine zusätzliche Allokation je WAL-Änderung") und die Zahlen als „Review-Messung, nicht committet". Verifikation V-4 (INFO) hielt fest, dass ein `HEAD`-Bezug im Diff-Stand-Befehl nur solange wahr bleibt, wie kein weiterer Commit `rowImage` in `*.go` bewegt. Beide Funde fand der Reviewer durch eigenes Nachmessen an beiden Ständen, nicht der Suchlauf des Implementers.

Quelle: `docs/reviews/review-slice-backfill-row-image-gemeinsam.md` (F-1, F-4) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-backfill-row-image-gemeinsam.md` (V-4). <!-- d-check:status-provenance -->
