**Vorgang:** slice-backfill-run-store (Verifikation V-1, V-2)

**Fund:** Zwei Zahl-Träger-Funde. (1) V-1 (LOW): `harness/sensors/db-adapter-coverage.md` führte den gemergten Nenner **813** (mit `postgresstorage` 472) als „Zustandsgröße dieses Gegenstands" samt Lauf des Vorgänger-Slice, `harness/sensors/coverage-gate.md` den Unit-Nenner **2082** mit `83.2%`; der Slice fügte DB-gestützten und netzlos geprüften Code hinzu, und am Stand des Diffs druckt derselbe Lauf `DB-Adapter-Coverage: 81.69% (gedeckt 830 von 1016 Statements; …)` (je PostgreSQL 17 und 18) und `coverage-gate` `84.70%`/`84.80%`/`84.90%` bei einem Nenner von 2398. Die Sätze trugen ihren Lauf und waren als Belege dieses Laufs wahr; die Überschrift „Zustandsgröße" beschrieb jedoch nicht mehr den Ist-Stand. Der Suchlauf des Slice suchte den bewegten Träger „Nenner" nicht, gefunden hat ihn der Verifier beim Nachmessen. Nachgezogen in der Closure mit Lauf und Commit-Kennung (`c7045f81`). (2) V-2 (INFO): im committeten Suchlauf-Feld stand „Diff sieben" Dateien für die Suche nach `cdc_admin`, gemessen sechs (`git grep -c` an `bd470a0b`); die drei genannten Bewegungen stimmten, die Zahl der Dateien nicht. Beide Funde fand der nachmessende Leser (Verifier), nicht der Implementer-Suchlauf.

Quelle: `docs/reviews/verifikation-slice-backfill-run-store.md` (V-1, V-2, §3.6 Tabelle). <!-- d-check:status-provenance -->
