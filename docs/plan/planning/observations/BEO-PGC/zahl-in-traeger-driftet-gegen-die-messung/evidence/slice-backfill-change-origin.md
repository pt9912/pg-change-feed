**Vorgang:** slice-backfill-change-origin (Verifikation V-1 und V-2, Planner-Closure)

**Fund:** Zwei Zahl-Träger-Funde am committeten §3.13-Suchlauf-Feld des Plans, beide in der Zelle „Anzahl-Formulierungen", beide aus Fixrunde 2 (die Zelle entstand nach dem Review; der Verifier war ihr erster unabhängiger Leser). (1) V-1 (LOW): die Zelle nannte für `git grep -n -i -E '(ten|zehn) (fields|felder)' <Stand> -- sdks examples` „je Stand 18 Treffer" als gemessen; der Verifier misst 16 an vier Ständen (`09386619`, `f4ba82ab`, `e95937cf`, `HEAD`), der Planner misst bei der Closure dasselbe (16 an allen vier Ständen, `sdks/` 16, `examples/` 0). Woher die 18 stammt, ist nicht rekonstruiert (die Zelle führt den Befehl mit der Tabellen-Maskierung `\|`, als ERE geschrieben liefert er 0). (2) V-2 (INFO): „24 Treffer (unverändert)" der erweiterten Suche stand für den Stand der Niederschrift, gegen `HEAD` misst der Verifier 25; der Zusatztreffer ist die vom Plan selbst angelegte Meldungszeile in `slice-backfill-sdk-origin` — eine Zelle, die ein Suchergebnis über Pläne führt, verschiebt sich mit der eigenen Meldung. Der Nachzug in der Closure setzt die Zahl 16 samt Ständen, kennzeichnet 25 als abgeleitet (24 + 1) und benennt den Stand. Beide Funde entstanden durch Nachmessen an mehreren Ständen, nicht durch den Suchlauf des Implementers.

Quelle: `docs/reviews/verifikation-slice-backfill-change-origin.md` (V-1, V-2, §3.3). <!-- d-check:status-provenance -->
