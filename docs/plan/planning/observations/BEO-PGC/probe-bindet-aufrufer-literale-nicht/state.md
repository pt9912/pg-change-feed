Zustand: offen — unter der Schwelle, kein Ausgang zugewiesen. Zähler (abgeleitet):
**1×** (evidence/slice-sdk-kotlin-cloudsmith.md). Der reale Tag-Lauf zeigte keinen
Namensfehler (beide Aufruf-Literale trugen dieselben Namen wie die Probe); die Grenze
besteht strukturell weiter. Mögliche Bindung, nicht gebaut: ein netzloser
Vergleichstest der Literale im Workflow gegen die des Dockerfiles (neue Datei in
`tools/harness/`, neues Target in `harness/mk/sdk.mk`, Zeile in `harness/README.md`
§Sensors) — Aufwand über dem Nutzen eines einzelnen Beleges.
