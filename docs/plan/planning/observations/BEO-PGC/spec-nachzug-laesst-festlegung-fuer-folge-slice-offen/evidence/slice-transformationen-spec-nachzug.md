**Vorgang:** slice-transformationen-spec-nachzug (Review F-1, MEDIUM; F-3, LOW; Verifikation §7 Lücken 1 bis 4 — ein Nachzug, eine Gelegenheit)

**Fund:** Der Nachzug legte Regelform, Fehlertexte und Randfälle fest, ließ aber den Bezeichner-Vergleich von `column`, `to` und Regelname offen (Review F-1: Groß-/Kleinschreibung, zulässige Zeichen, Eindeutigkeit der Adresse `schema.table.name`) und die Fehlertext-Tabelle ohne Zeile für einen fehlenden `rule_spec` und ohne Trennung von „unbekannter Regeltyp“ und „Pflichtschlüssel fehlt“ (F-3). Beide sind in der Fixrunde festgelegt, mit Bestands-Belegen (`containsName`, `ColumnExists`, `identifierShape`). Der Verifier stellte danach die Frage „kann ein Implementer `slice-transformationen-kern-rename` aus der Spec umsetzen?“ und fand vier weitere Lücken: die Form-Prüfungen waren keiner Schicht zugeordnet, ein Domänen-Sentinel für „Zielname gleich Quellname“ hatte keine Abbildung auf den K3-Text, die Regeltyp-Menge am Antragsweg stand gegen die gestaffelte Lieferung ungeklärt, die SQL-Funktionen trugen keine Parameterliste. Alle vier sind in den Plänen der Folge-Slices festgelegt.

Quelle: Review-Report zu `slice-transformationen-spec-nachzug` (F-1, F-3) · Verifikations-Report zu `slice-transformationen-spec-nachzug` (§7), beide unter `docs/reviews/`.
