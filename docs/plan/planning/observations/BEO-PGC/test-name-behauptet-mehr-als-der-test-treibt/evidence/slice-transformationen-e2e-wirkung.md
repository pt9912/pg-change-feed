**Vorgang:** slice-transformationen-e2e-wirkung (Review F-3, LOW; Verifikation §5 Zeile F-3)

**Fund:** Der Doc-Kommentar von `TestE2ETransformationConflictsFailWithSpecText` (`test/integration/transformation_e2e_test.go`) sagte: „Jeder Negativfall weicht in genau einem Feld von der Gegenprobe ab, die `applied` endet.“ Fünf der sechs Fälle sind `set_transformation`-Anträge, die in Regelname, Spalte oder Zielname von der Gegenprobe abweichen; der sechste (K4, Regelname nicht geführt) ist ein `remove_transformation`-Antrag — andere Antragsart, kein `rule_spec`. Seine Gegenprobe (Entfernen einer geführten Regel endet `applied`) läuft nur im `t.Cleanup` von `removeRulesAtEnd` und wird dort nur bei nicht fehlgeschlagenem Test als Fehler gemeldet. Die Aussage trug für die Set-Fälle, für den Remove-Fall nur indirekt. Die Fixrunde (`4800d75b`) benennt die Menge: fünf Set-Fälle in genau einem Feld, der Remove-Fall mit nicht geführtem Regelnamen und seiner Gegenprobe im Aufräumen; die Verifikation maß je Fall das eine Feld nach (K1 Regelname, K2 Spalte, K3 zweimal Zielname, K4 Spalte) und fuhr die Mutationen G1 bis G4 rot.

**Form (Ausprägung):** dieselbe Klasse wie beim Zweitauftreten — eine Aussage „jeder“ über eine Menge (hier sechs Fälle), von der der Test eine Teilmenge (fünf) so übt, wie der Satz sie beschreibt; der Träger ist hier der **Doc-Kommentar** der Testfunktion, nicht der Name. Schwere LOW, vor dem Merge vom Reviewer gefunden. Der Eintrag erreicht mit dieser Datei **3×**; der Lese-Schritt der Closure von `welle-transformationen` liest ihn.

Quelle: `docs/reviews/review-slice-transformationen-e2e-wirkung.md` (F-3) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-transformationen-e2e-wirkung.md` (§5 Zeile F-3, §4 G1 bis G4). <!-- d-check:status-provenance -->
