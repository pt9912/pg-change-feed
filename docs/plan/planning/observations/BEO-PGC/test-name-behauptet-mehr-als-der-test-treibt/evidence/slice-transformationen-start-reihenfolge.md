**Vorgang:** slice-transformationen-start-reihenfolge (Review F-4, LOW; Verifikation §5 Zeile F-4)

**Fund:** Zwei Tests in `internal/bootstrap/administration_startorder_internal_test.go` trugen Namen, die eine Lauf-Reihenfolge zusagten (`TestRunReconcilesAndStartsTheBackfillWorkerBeforeTheAdministrationPass`, `TestRunCallsTheStreamOnlyThroughTheOrderedSequence`). Beide parsen den Quelltext von `wiring.go` und vergleichen Positionen der Aufrufe; gelesen wird die Quelltext-Reihenfolge, kein Lauf. Der Reviewer nannte zwei schädliche Umformungen, die sie nicht fangen (ein Aufruf hinter einer toten Verzweigung, ein nie aufgerufenes `go func`-Literal, aus dem Test gelesen), und eine harmlose, die sie rot färbt (das Herauslösen in eine Hilfsfunktion). Die Fixrunde (`eb4c2b1c`) benannte sie nach der Messung (`TestRunSourceTextPassesStreamRunOnlyAsArgumentOfTheSequence`, `TestRunSourceTextOrdersReconcileAndWorkerStartBeforeTheSequenceCall`), der Godoc nennt die Grenze im Ist-Ton. Die Verifikation fuhr die tote Verzweigung an einer Kopie (Mutation V14: der Aufruf von `reconcileBackfillRuns` in `if false { … }`) und sah beide Tests grün — die Blindheit ist gemessen, und der Plan nennt sie in §3.

**Form (Ausprägung):** derselbe Mechanismus wie in den Vorgängern (der Name benennt mehr, als der Test treibt), diesmal an einem **Test, der den Quelltext liest**: der Name behauptet eine Lauf-Reihenfolge, der Test bindet Text-Positionen. Schwere LOW, vor dem Merge vom Reviewer gefunden. Der Eintrag steht mit dieser Datei bei **4×** (offen, Schwelle seit dem dritten Beleg erreicht, Ausgang nicht zugewiesen).

Quelle: `docs/reviews/review-slice-transformationen-start-reihenfolge.md` (F-4) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-transformationen-start-reihenfolge.md` (§4 V14, §5 Zeile F-4). <!-- d-check:status-provenance -->
