**Vorgang:** slice-transformationen-start-reihenfolge (Review F-3, MEDIUM; Verifikation V-4, LOW)

**Fund:** Der Reviewer las im Code: der Stream-Start wartet auf den vollständigen Vorlauf; die einzigen Grenzen des Wartens sind der Prozess-`ctx` und die WAL-Fehlerschwelle (`stopStream`, über `streamCtx`); eine Zeit-Grenze je Antrag oder je Durchlauf gibt es nicht. Ein Antrag, der lange läuft, ohne zu hängen, oder eine lange Queue hält die gesamte Erfassung der Quelle an; die Lebenszeichen-Goroutine startet vor der Sequenz, der `--healthcheck` liest nur ihr Alter (`heartbeatStaleAfter`), ein wartender Vorlauf erscheint als gesund (beides hergeleitet, nicht am laufenden Prozess gemessen). Der Halte-Test bindet, dass ein endender Kontext den Vorlauf beendet. Nicht benannt waren im Godoc und im Plan die einzige Grenze (WAL-Schwelle), der Zustand „gesund“ während des Wartens und die Abweichung der beiden Kontexte (Vorlauf unter `streamCtx`, Goroutine unter `administrationCtx`). Die Fixrunde (`eb4c2b1c`, `cd5ff4f2`) benennt Grenze, fehlende Frist, Kontext-Semantik und Betreiber-Folge im Godoc und in Plan §6. Die Verifikation (V-4) stellt fest: keine DoD-Verletzung (DoD 1 und `ADR-0112` verlangen keine Frist), die Entscheidung über eine Zeitgrenze trifft weder der Implementer noch der Verifier; die Frage stand nur im Bericht der Fixrunde und damit an keinem committeten Träger. Der Planner legt in der Closure den Eintrag an.

**Form:** eine **Wartegrenze ohne Zeitgrenze** an einer Stelle, an der das Warten die Erfassung anhält und für den Betreiber nicht sichtbar wird. Schwere MEDIUM (Review) und LOW (Verifikation, Träger fehlte), vor dem Merge von Reviewer und Verifier gefunden.

Quelle: `docs/reviews/review-slice-transformationen-start-reihenfolge.md` (F-3) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-transformationen-start-reihenfolge.md` (§5 Zeile F-3, §8 V-4, §9). <!-- d-check:status-provenance -->
