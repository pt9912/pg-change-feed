**Vorgang:** slice-backfill-change-origin (Implementer-Befund, Planner-Closure)

**Fund:** Beim Schreiben der neuen Store-Tests (`TestPersistAndReadCarryChangeOrigin` in `store_test.go`, der View-Test in `sqlviews_test.go`) stellte der Implementer fest, dass ein `t.Fatalf` innerhalb einer Schleife über offene `pgx.Rows` das `pool.Close()` im Cleanup blockiert; die neuen Tests halten die Verbindung über `defer rows.Close()` frei und tragen die Begründung im Kommentar. Die Aussage ist aus dem Implementer-Bericht übernommen, nicht vom Planner reproduziert. Bei der Closure gemessen: die zwei vorbestehenden `pool.Query`-Stellen in `sqlviews_test.go` (Zeilen 229 und 253) tragen dieselbe Form ohne `defer`. Die bestehenden Store-Tests sind nicht auditiert; weder Reviewer noch Verifier haben den Befund geführt (kein Diff-Fund, der Diff trägt die Abhilfe).
