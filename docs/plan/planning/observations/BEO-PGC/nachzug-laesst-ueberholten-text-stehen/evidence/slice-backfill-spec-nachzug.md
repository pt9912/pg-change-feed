**Vorgang:** slice-backfill-spec-nachzug (Review-Fund F-1, Planner-Closure)

**Fund:** Der Spec-Nachzug ergänzte in `spec/pflichtenheft.md` bei `SPEC-022` die neue Zeile „Position und `limit`" (die Fortsetzung `from = <letzte gelieferte commit_position> + 1` liefert bei einer Position mit mehr Changes als `limit` den Rest nicht), ließ aber die Zeile „Reihenfolge" vier Zeilen darüber im selben Abschnitt mit der unbedingten Fortsetzungs-Regel und ohne Verweis stehen — zwei Aussagen in einer Tabelle, keine verweist auf die andere. Der Reviewer fand den Widerspruch (F-1, MEDIUM), die Fixrunde (`1034840f`) setzte den Vorbehalt und den Verweis auf die Zeile „Position und `limit`"; der Verifier bestätigte die Behebung an der Zeile selbst und am Code (`queries.SelectChanges` schneidet mit `LIMIT` Zeilen, nicht Positionen). Dritter Beleg der Klasse, dritter Träger-Typ: eine Tabellenzeile des Pflichtenhefts (nach dem Slice-Plan-Absatz und dem Pflichtenheft-Fließtext der ersten zwei Belege). Ursprung und Vorkommen fallen zusammen — der Nachzug selbst erzeugte den Widerspruch im selben Zug; der committete §3.13-Suchlauf führte die Fortsetzungs-Aussage erst mit der Fixrunde als bewegte Eigenschaft.
