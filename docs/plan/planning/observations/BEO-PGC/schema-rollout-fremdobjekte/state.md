Zustand: offen — kein Träger bislang; eine Lösung bräuchte entweder die
beiden Views ins neutrale Modell (dieselbe Überführung wie bei den drei
jetzt retirierten Views, slice-016) oder einen dokumentierten
`--allow-destructive`-Umgang für Fremdobjekte im `schema-rollout`-Target.
Zähler (abgeleitet): 1× (evidence/slice-016.md) — unter der
3×-Schwelle, kein Ausgang fällig.
