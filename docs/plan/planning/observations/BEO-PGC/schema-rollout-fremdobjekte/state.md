Zustand: offen — kein Träger bislang; eine Lösung bräuchte entweder die
vier Fremdobjekte (inzwischen zwei Views UND zwei Funktionen, siehe
evidence/slice-063.md) ins neutrale Modell überführen (bei den Funktionen
bislang an `POST_EXECUTE_DRIFT`/Exit 5 gescheitert) oder einen
dokumentierten `--allow-destructive`-Umgang für Fremdobjekte im
`schema-rollout`-Target. Zähler (abgeleitet): 2× (evidence/slice-016.md,
evidence/slice-063.md) — unter der 3×-Schwelle, kein Ausgang fällig.
