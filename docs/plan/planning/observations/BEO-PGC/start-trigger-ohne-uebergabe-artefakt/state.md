Zustand: offen — Ausgang noch nicht zugewiesen (unter der 3×-Schwelle). Kein
Träger ist vorgeschlagen: ob ein einzelner Vorgang eine geschärfte Regel
rechtfertigt (z. B. eine mechanische Prüfung, dass ein Slice-Plan mit
Architect-Zug als Start-Bedingung diesen auch als Artefakt referenziert,
bevor `git mv` nach `in-progress/` läuft), entscheidet der Lese-Schritt bei
3×; bei 1× nicht fällig.

Zähler (abgeleitet): 1× (evidence/slice-d-check-tracked-modul.md).
