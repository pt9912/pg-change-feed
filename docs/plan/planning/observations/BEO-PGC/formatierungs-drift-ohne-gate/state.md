Zustand: offen (**1×**) — unter der Schwelle, kein Ausgang zugewiesen. Die vier Dateien
des Diffs sind in der Fixrunde formatiert (Verifikation: `gofmt -l internal tools cmd` im
gepinnten Toolchain-Image listet nur `receive/seam_test.go` und `outbound/log_test.go`,
beide Bestand, beide nicht im Diff). Ein Träger ist nicht vorgeschlagen; ein `gofmt`-Gate
wäre eine Entscheidung mit ADR, die über diesen Eintrag hinausgeht.

Zähler (abgeleitet): **1×** (evidence/slice-backfill-sql-administration.md).
