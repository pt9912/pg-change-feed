**Vorgang:** slice-transformationen-antragsweg-usecase (Review F-3, LOW; Review F-10, INFO als Ursache)

**Fund:** Drei Dateien des Diffs waren nicht `gofmt`-konform, am Parent waren sie es: `wiring.go` (der neue Block `setTransformations:`/`removeTransformations:` im Literal von `administrationDeps` in `Run` stand durch eine Leerzeile getrennt, die sechs folgenden Felder waren nicht auf die Breite des längsten Schlüssels ausgerichtet), `transformationspec.go` (die Aufzählung a) bis d) im Doc-Kommentar von `ParseTransformationSpec` ist keine gültige Liste im Doc-Kommentar-Format) und `administrationrequest_test.go` (Ausrichtung der Konstanten im `const`-Block). Kein Gate deckt `gofmt`; die Form der Abweichung im Literal (Leerzeile, ungleiche Breite) ist die einer skriptgesteuerten Einfügung — der Implementer meldete, mehrfach `sed -i` gegen Repo-Dateien benutzt zu haben (F-10, Prozessverstoß gegen die Nutzerregel und `AGENTS.md` §3.1). Die Fixrunde formatierte die drei Dateien (`gofmt -w`, Docker-only über das gepinnte Toolchain-Image); `gofmt -l` nennt danach nur Bestand.

**Form (Ausprägung):** dieselbe Klasse (der Diff bringt neue Abweichungen, kein Gate liest sie, allein der Reviewer), mit einer neuen Ursache: ein **Textwerkzeug am Repo** statt des Editors des Implementers, und ein Doc-Kommentar-Format (Liste), das `gofmt` seit Go 1.19 normiert, neben der Feldausrichtung. Gefunden hat es der Reviewer durch `gofmt -l` im Toolchain-Container; der Verifier maß dasselbe (nur `queries/queries.go`, am Parent bereits formabweichend). Vor dem Merge.

Quelle: `docs/reviews/review-slice-transformationen-antragsweg-usecase.md` (F-3, F-10) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-transformationen-antragsweg-usecase.md` (§1 Zeile gofmt, §5 Zeilen F-3 und F-10). <!-- d-check:status-provenance -->
