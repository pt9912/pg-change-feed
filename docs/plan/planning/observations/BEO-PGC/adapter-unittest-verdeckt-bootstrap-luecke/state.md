Zustand: offen — Ausgang: **weiter offen**. Ein möglicher Träger wäre ein
schlanker Bootstrap-Smoke-Test (`ConfigFromEnv` + `Run()`-Teilaufruf gegen
einen minimalen ENV-Satz, der prüft, dass kein Adapter-Konstruktor einen
`nil`-Use-Case erhält) — das entschiede aber erst ein Architect-Zug, kein
Slice hat das bislang vorgeschlagen. Zähler (abgeleitet): 1×
(evidence/slice-061.md).
