**Vorgang:** slice-backfill-change-origin (Review-Fund F-2, Planner-Closure)

**Fund:** Der Guard-Test `tools/harness/run-schema-rollout-guard-test.sh` nannte seinen Lauf 6 im Kopf „der Beleg, dass die Wache nicht pauschal durchlässt" (dieselbe Aussage in der Sensors-Zeile `make schema-rollout` von `harness/README.md`), die Assertion prüfte aber nur, dass der `make`-Exit ungleich 0 ist. Der Reviewer setzte die Mutation selbst: mit entfernter `knownForeignObjects`-Prüfung in `tools/schema/rolloutguard/guard.go` meldet die Wache „alle Blocker sind bekannte Fremdobjekte", das Target läuft mit `--allow-destructive`, und d-migrate bricht den `DropColumn`-Blocker trotzdem selbst ab — der Guard-Test endet Exit 0, die Mutation überlebt. Form der Klasse: **Assertion** (ein Negativlauf, dessen Abbruch aus einem fremden Pfad kommt); die Aussage über die Wache war wahr (die Unit-Tests `TestDecideRefusesUnknownDestructiveBlocker` u. a. färben die Mutation rot), falsch war die Stütze. Die Fixrunde (`e3d55e2b`) führt Lauf 6a ein (eine nicht deklarierte Funktion `cdc.zz_rolloutguard_unbekannt()`, Blocker derselben Klasse wie die sechs bekannten Fremdobjekte; der Beleg ist ihr Fortbestehen nach einem Lauf mit d-migrate-„Error 8"), Lauf 6b bleibt als Beleg des d-migrate-Abbruchs gegen einen real gemeldeten Blocker und wird im Kopf nicht mehr als Beleg der Bekannt-Liste geführt. Der Verifier maß die Mutation nach: mit deaktivierter Prüfung endet der Guard-Test an Lauf 6a mit Exit 1 („Lauf 6a lief durch (Exit 0), obwohl ein unbekannter destruktiver Blocker vorlag"). Ein Reviewer-Nachprüfungs-Report zur Fixrunde liegt nicht vor; die Nachmessung trägt der Verifier.

Quelle: `docs/reviews/review-slice-backfill-change-origin.md` (F-2, Mutation G5) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-backfill-change-origin.md` (§5, Zeile G5). <!-- d-check:status-provenance -->
