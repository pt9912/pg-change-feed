**Vorgang:** slice-backfill-run-usecase (Review-Fund F-6, Verifikation V-2, Nachmessung der Closure)

**Fund:** Die Befehle im Suchlauf-Feld des Plans trugen ihren Satz nicht, in drei Schichten. (1) Der Befehl der Zeile „Beschreibung der drei Adapter" lieferte aus der Zelle kopiert 0 Treffer, weil `-E` das Tabellen-Escape `\|` als wörtliches Pipe-Zeichen las (Review F-6); die Fixrunde behob die Abgrenzung der Zeilen, nicht die Kopierbarkeit (Verifikation V-2: 0 gegen 11 Zeilen, ebenso die Zeile der Transformations-Pläne, 0 gegen 6). (2) Der Zähl-Befehl nannte `\| wc -l` mit demselben Escape. (3) Der Filter „ohne historische Träger" (`grep -v -E` über die `-n`-Ausgabe) schloss nach dem **Zeileninhalt** aus, nicht nach dem Pfad; die Behauptung „nur dieser Plan" traf nicht zu, eine zweite Datei (`slice-transformationen-antragsweg-usecase`, eine Zeile über eine neue Datei in `port/outbound`) stand im Ergebnis. Behoben: alle Befehle tragen mehrere `-e`-Argumente statt einer Alternation, Zähl-Befehle nutzen `git grep -c`, die Herkunft ist ein Pfad-Ausschluss (`':!…'`); die Zahlen sind an den drei genannten Ständen neu gemessen. Der Reviewer fand (1), der Verifier die Kopierbarkeit, die Nachmessung der Closure die Schicht (3).

Quelle: `docs/reviews/review-slice-backfill-run-usecase.md` (F-6) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-backfill-run-usecase.md` (V-2, §5). <!-- d-check:status-provenance -->
