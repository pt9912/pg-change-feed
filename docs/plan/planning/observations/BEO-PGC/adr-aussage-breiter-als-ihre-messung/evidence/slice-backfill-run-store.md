**Vorgang:** slice-backfill-run-store (Review-Fund F-1, Verifikation §5 Zeile `ADR-0047`, Plan §6 „an Architect gemeldet")

**Fund:** `ADR-0113` (`Accepted`) Festlegung 1 setzt die Annahme als `cdc_admin` voraus — `INSERT` der Run-Zeile und `UPDATE … applied` des Antrags in **einer** Transaktion — und sagt, die bestehende Schleife `processAdministrationRequests` bleibe „unverändert lauffähig". Der Beleg der Festlegung deckt die Rechte auf `cdc.backfill_run` (Grants `SELECT, INSERT` für `cdc_admin`); das Recht von `cdc_admin` auf `cdc.administration_request` steht weder in der ADR noch in `tools/schema/nacharbeit-roles.sql`, und `ADR-0047` führt in ihrer Rollen- und Aufrufer-Tabelle keine Antrags-Queue (ihre Ist-Stand-Tabelle nennt `source_table`, `schema_version`, `consumer`, `consumer_position`, `transaction`, `change`). Gemessen vom Reviewer an PostgreSQL 18 vor der Fixrunde: `cdc_admin` trug `SELECT` und `UPDATE` auf die Queue nicht, `relacl` leer; der Verifier maß nach der Fixrunde an 17.11 und 18.6 `SELECT` und `UPDATE` als gesetzt. Der Slice setzte den Wortlaut korrekt um; die Aussage war breiter als die Menge der Tabellen, an der sie geprüft war. Behoben im Betrieb der Rollen (Grant `SELECT, UPDATE`), nicht im Wortlaut: beide ADRs sind `Accepted` und unberührbar, ob sie eine Ergänzung brauchen und ob die Grants-Tabelle von `SPEC-029` (nur `cdc.backfill_run`) den Vermerk auf `cdc.administration_request` nennt, ist als Frage an den Architect gemeldet (Plan §6). Verwandt, hier nicht als zweiter Beleg gezählt: `BEO-PGC/architect-verdikt-rollen-scope-luecke` (dieselbe Familie — der Grant am Nachbar-Objekt eines neuen Schreibpfads bleibt ungeprüft; dort ist die Ursache ein Verdikt-Ablauf, hier der Wortlaut einer `Accepted` ADR).

Quelle: `docs/reviews/review-slice-backfill-run-store.md` (F-1, „Prüfungsteile zur Frage (a)–(c)") <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-backfill-run-store.md` (§3.2, §5 Zeilen `ADR-0047`, `ADR-0113` und `SPEC-029`, V-7). <!-- d-check:status-provenance -->
