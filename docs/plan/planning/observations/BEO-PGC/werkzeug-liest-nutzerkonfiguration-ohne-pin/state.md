Zustand: offen (**1×**) — unter der Schwelle, kein Ausgang zugewiesen. Der Vorgang selbst ist
behoben (Pinnung der Form des Stroms im Aufrufer, Exit 2 im Programm bei fremdem Präfix, Test des
Aufrufers unter fremder Git-Konfiguration; Verifikation `verifikation-slice-code-kommentare-kennungen`
§7). Kandidat für eine geschärfte Regel: ein Werkzeug, das die Ausgabe eines Fremdwerkzeugs parst,
pinnt die Form dieser Ausgabe gegen die Konfiguration des Aufrufers und endet mit Exit 2, wenn die
Form nicht erkannt wird, statt „kein Befund“ zu melden; sein Test bindet den Strom an eine fremde
Konfiguration. Ob die Klasse über dieses Werkzeug hinaus trägt, zeigt ein zweites Auftreten.
Trigger für ein zweites Auftreten: ein weiteres Werkzeug, das die Ausgabe von `git` (oder eines
anderen konfigurierbaren Werkzeugs) parst.
Zähler (abgeleitet): **1×** (evidence/slice-code-kommentare-kennungen.md).
