**Vorgang:** slice-transformationen-e2e-wirkung (Review F-2, MEDIUM; Verifikation §5 Zeile F-2, Mutationen R1 und R2)

**Fund:** Die Runner-Phase „Neustart und Ausschluss“ sagt zu, dass Regelstand **und** Ausschluss einen realen Neustart überleben („vor und nach dem zweiten Neustart weder name noch customer_name noch den Wert“). Nur der **erste** `docker restart` trug den Beleg „Startzeit davor ≠ Startzeit danach“; vor dem zweiten stand kein Vergleich. Der Reviewer mutierte die Eingabeseite des zweiten Neustarts (die Zeile `docker restart "$FEED_CONTAINER"` an der zweiten Stelle entfernt, das folgende `bf_await_healthy` bleibt): Exit 0, die Phase druckte weiter „…vor und nach dem zweiten Neustart weder name noch customer_name noch den Wert“ (Review, Mutation R10). Die Kombination aus Ausschluss und `rename_column`-Regel wird nur an dieser Stelle nach einem Neustart gelesen. Die Fixrunde (`4800d75b`) zog beide Neustarts durch `tf_restart_feed`, der die Startzeit davor und danach liest und die Phase bei Gleichheit beendet; die Verifikation mutierte beide Aufrufe einzeln (R1: erster, R2: zweiter Neustart übersprungen), beide rot mit „die Startzeit des Feed-Containers blieb nach docker restart gleich“, und zwei Produktivcode-Mutationen der Bindung des Prozessstarts (P1: Regelstand, P2: Ausschlussstand beim Prozessstart verworfen) färbten Phase 2 rot.

**Form (Ausprägung):** derselbe Mechanismus wie im Erstauftreten (die Aussage hängt an einer Eingabe, die keine Mutation färbt), in einer **Runner-Phase** und an einer **realen Aktion** statt an einem Fake: der Neustart selbst war die ungebundene Eingabe, nicht ein Rückgabewert. Schwere MEDIUM (der Aufruf stand wörtlich im Skript und endete bei einem Fehlschlag unter `set -e`; der Skill führt die Klasse unter HIGH), daher Datei trotz Deckel; vor dem Merge vom Reviewer gefunden, Ausgang unverändert **verkörpert**.

Quelle: `docs/reviews/review-slice-transformationen-e2e-wirkung.md` (F-2, Mutation R10) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-transformationen-e2e-wirkung.md` (§4 R1, R2, P1, P2; §5 Zeile F-2). <!-- d-check:status-provenance -->
