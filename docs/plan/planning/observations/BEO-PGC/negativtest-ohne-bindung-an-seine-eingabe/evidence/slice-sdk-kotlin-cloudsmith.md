**Vorgang:** slice-sdk-kotlin-cloudsmith (Implementer-Lauf, Mutations-Tabelle Schritt 19; kein Review-Finding)

**Fund:** Die Probe der Publish-Konfiguration im Docker-Bau (`sdks/kotlin/Dockerfile`, Stufe `pack`) prüfte in ihrer ersten Fassung nur, dass `./gradlew --dry-run publishMavenPublicationToCloudsmithRepository publishMavenPublicationToGitHubPackagesRepository` mit Exit 0 endet. Die Mutation der Eingabeseite (`name = "Cloudsmith"` zu `"CloudsmithX"` in `build.gradle.kts`) blieb **grün**: Gradle löst abgekürzte Aufgabennamen auf, der `--dry-run`-Lauf druckte `:publishMavenPublicationToCloudsmithXRepository SKIPPED`. Die Fassung war grün ohne Aussage. Ersetzt: die Probe liest die aufgelösten Aufgabennamen der `--dry-run`-Ausgabe und verlangt `:<Name> SKIPPED` als ganze Zeile; die fünf Mutationen der Tabelle färben sie danach rot. **Neue Form** der Klasse: der Träger ist eine Probe im Docker-Bau (Werkzeug-Abkürzungsregel als Eingabeseite), kein Test im Go-Paket. Gefunden hat es der Implementer durch die Mutation, vor dem Review; die verkörperte Regel hat gewirkt.

Quelle: `docs/plan/planning/done/slice-sdk-kotlin-cloudsmith.md` §3 (Plan-Nachzug des Implementers, Mutations-Tabelle) · `sdks/kotlin/Dockerfile` (Stufe `pack`).
