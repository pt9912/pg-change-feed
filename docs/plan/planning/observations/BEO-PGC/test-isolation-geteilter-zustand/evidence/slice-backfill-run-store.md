**Vorgang:** slice-backfill-run-store (Review F-3, Plan §3 Zeile `schema.sql`)

**Fund:** Die neuen Store-Tests des Slice (`backfill*_test.go`, `roles_test.go`) laufen gegen das ausgerollte Schema und bereinigen nur Zeilen unter ihren eigenen Kennungen (Präfix je Test); sie laufen aber nur, weil ihre Dateinamen vor den Dateien sortieren, deren Tests das Schema per `DROP SCHEMA cdc CASCADE` samt hand-DDL neu aufbauen (`store_test.go` Zeile 50, `tableactivation_test.go` Zeile 39 — mit `grep -n "DROP SCHEMA" *_test.go` gemessen). Damit entsteht dieselbe Ausführungsreihenfolge-Kopplung, die der Eintrag als Workaround beschreibt (Dateinamen-Sortierung statt Behebung des geteilten Zustands): der Kopplungs-Kommentar in `backfillhelpers_test.go` nannte `consumerstate_test.go` als Schema-Neuaufbau-Test, die Ausführenden sind aber zwei andere Dateien, und die Kopplung der `roles_test.go`-Tests stand nirgends (F-3, LOW). Behoben in der Fixrunde: der Kommentar nennt die zwei Dateien und die Dateinamen, die davor sortieren; die Ursache (unskopierter `DROP SCHEMA`) bleibt unberührt. Benannt, nicht gezählt: die zwei Rollen-Tests des Bestands (`TestCdcCaptureRoleWritesChangesNotSchema`, `TestCdcAdminRoleManagesSchemaNotChanges`) schreiben unter festen Kennungen ohne Bereinigung — ein zweiter Lauf gegen dieselbe Instanz träfe den Primärschlüssel; jeder `make test-store`-Lauf startet einen frischen Container (Review F-9, Bestand, von diesem Slice nicht bewegt).

Quelle: `docs/reviews/review-slice-backfill-run-store.md` (F-3, F-9) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-backfill-run-store.md` (§8 Zeile 4). <!-- d-check:status-provenance -->
