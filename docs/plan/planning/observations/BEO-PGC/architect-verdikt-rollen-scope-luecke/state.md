Zustand: offen — geprüft in der Closure von `welle-backfill-bestand`: das Verdikt
`architect-verdict-backfill-schema-klasse-rollen` behandelt die Rollen-Konsequenz für
seinen Fall (§3, Rollenschnitt der Antrags-Queue), setzt aber keinen Pflichtabschnitt
im Verdikt-Ablauf; der Eintrag bleibt offen (1×).

Kein Träger bislang; eine geschärfte Regel gehört in den
Architect-Verdikt-Ablauf selbst (z. B. ein Pflichtabschnitt „Rollen-/
Grant-Konsequenz" neben Domain-/Port-/ADR-Fragen, sobald eine Frage einen
neuen physischen Schreib- oder Löschpfad einführt). Zähler (abgeleitet):
1× (evidence/slice-044.md) — **unter der 3×-Schwelle**, kein Ausgang
fällig.
