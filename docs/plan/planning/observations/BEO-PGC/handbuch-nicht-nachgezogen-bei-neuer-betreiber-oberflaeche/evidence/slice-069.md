**Vorgang:** slice-069
**Fund:** Der Slice führte `CDC_GRPC_ADDR` (gRPC-Live-Stream) ein und ließ
das Handbuch unberührt — der Implementer benannte den Aufschub ausdrücklich
(„die HTTP-ENV-Variablen sind dort ebenfalls nicht dokumentiert;
Operator-Doku gehört zur Erreichbarkeit in `slice-071`/`slice-072`"). Damit
ist der Aufschub benannt, die Lücke bleibt aber offen (kein Treffer für
`CDC_GRPC_ADDR` im Handbuch, geprüft 2026-09-14).
