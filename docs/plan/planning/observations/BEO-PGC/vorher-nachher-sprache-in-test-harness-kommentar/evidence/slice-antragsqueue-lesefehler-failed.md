**Vorgang:** slice-antragsqueue-lesefehler-failed (Review F-1, HIGH; F-8, INFO im selben Diff)

**Fund:** Der Doc-Kommentar von `sqlexec.ReadPendingRequests` (`internal/adapters/driven/postgresstorage/sqlexec/translate.go`) begründete die Durchreichung einer verworfenen Zeile mit der verworfenen Alternative im Konjunktiv: „statt die Lesung zu beenden — ein Fehler der Lesung hielte jeden Antrag dahinter an“. Derselbe Halbsatz stand am Parent im Konstruktor-Kommentar (`internal/domain/model/administrationrequest.go`, „ein abgelehnter Lesevorgang hielte jeden Antrag dahinter an“), wurde dort entfernt und am neuen Ort neu geschrieben. Eine zweite Stelle im geänderten Block über `processAdministrationRequests` (`internal/bootstrap/wiring.go`, „ein `pending` bleibender Antrag würde jeden Durchlauf erneut versuchen“) trug dieselbe Form als Bestand (F-8, INFO). Der Reviewer stufte F-1 nach der Skopus-Klausel des HIGH-Punkts „Kommentar trägt keine der Kommentar-Klassen“ als **HIGH** ein; die Fixrunde formulierte beide Stellen im Indikativ über die Zusage der Stelle („steht als `Rejected` … an ihrer Stelle der Ordnung; die Lesung endet nur bei einem Fehler der Anfrage, des Scans oder der Iteration“; „die Abfrage liest nur `pending`“). Vor dem Merge gefunden.

**Form (Ausprägung):** dieselbe Rhetorik wie die vier bisherigen Belege (verworfene Alternative als Nebenklausel im Konjunktiv), **im Produktionscode** statt in einem Test-Harness-Skript; der Name des Eintrags ist der des Erstauftretens, sein Mechanismus gilt für jede Datei, die der Skopus der Klausel nennt (Code, Konfiguration, Skripte, Tests, Runner). Der Implementer prüfte Schritt 20 (Probe als Frage, Kandidatenlauf nur auf Slice-Nummern und „vor/nach diesem Slice“) und fand beide Stellen nicht; ein Kandidatenlauf auf Konjunktivformen am Diff ist gemessen (gedruckte Zeile, Lauf bei der Closure): `git diff fc107f42 5d8e0748 -U0 -- '*.go' | grep -E '^\+.*//' | grep -c -i -E 'wäre|würde|hielte|hätte'` trifft 2 Zeilen, beide die Defekte dieses Fundes; am Stand nach der Fixrunde (`fc107f42..HEAD`) 0 Treffer. Mit `statt|sonst` im Muster stiege der Lauf auf 13 Zeilen, davon 2 Defekte (Ursprung: Lauf bei der Closure, `grep -i -E 'wäre|würde|hielte|hätte|sonst|statt'` gegen `5d8e0748`).

Quelle: `docs/reviews/review-slice-antragsqueue-lesefehler-failed.md` (F-1, F-8) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-antragsqueue-lesefehler-failed.md` (§7 Zeilen F-1, F-8). <!-- d-check:status-provenance -->
