**Vorgang:** slice-transformationen-e2e-wirkung (Review F-1, HIGH)

**Fund:** Der Kommentar vor dem ersten `docker restart` der Runner-Phase „Neustart und Ausschluss“ in `tools/harness/run-integration-tests.sh` schloss mit einem Konjunktiv über den Zustand ohne die Zusage der Stelle: „ohne die Ableitung des Regelstands beim Prozessstart trüge die nächste Change die Rohform“. Die Kommentar-Klasse (Zusage, Kopplung, Abgrenzung, Rang-Zeiger, Grenze) trägt der Halbsatz nicht; die Zusage der Stelle stand im ersten Halbsatz und im letzten Satz. Der Reviewer fand ihn vor dem Merge und stufte nach der Skopus-Klausel des Skill-Punkts als HIGH ein. Die Fixrunde (`4800d75b`) formulierte den Kommentar im Indikativ: „der Prozessstart leitet den Regelstand aus den applied-Zeilen ab, und die Form der nächsten Change ist der Beleg dieser Ableitung“; der Verifier las den Kommentar und fand über alle hinzugefügten Kommentarzeilen von Runner und Go-Datei keine weitere Konjunktiv- oder Vorher-Nachher-Zeile.

**Form (Ausprägung):** dieselbe Klasse in einem **Runner-Skript** (Skopus der Klausel: `tools/harness/*.sh`) — mit einer Lücke im Kandidatenlauf: der diff-skopierte Lauf aus `implement-slice` Schritt 20 (Wortliste `wäre|waere|würde|wuerde|hielte|hätte|haette|sonst|statt` hinter `//` oder `#`) druckt am Stand des Implementers vor dem Review (`66f60c8b`, Diff seit `c246ba4f`) **0** Zeilen (gemessen bei der Closure: `git diff -U0 c246ba4f 66f60c8b -- '*.go' '*.sh' '*.awk' | grep -cE '^\+.*(//|#).*(wäre|waere|würde|wuerde|hielte|hätte|haette|sonst|statt)'`), obwohl die Zeile mit „trüge“ im Diff steht (`git diff -U0 c246ba4f 66f60c8b -- '*.sh' | grep -c 'trüge'`: 1). Die Wortliste ist eine endliche Reihe von Konjunktiv-II-Formen; „trüge“ und andere Formen fehlen. Ausgang des Eintrags unverändert **verkörpert**; die Lücke der Wortliste ist benannt (Adresse: der Lese-Schritt der Closure von `welle-transformationen`, der den Eintrag bei mehr als 3× liest).

Quelle: `docs/reviews/review-slice-transformationen-e2e-wirkung.md` (F-1) <!-- d-check:status-provenance -->
· `docs/reviews/verifikation-slice-transformationen-e2e-wirkung.md` (§5 Zeile F-1). <!-- d-check:status-provenance -->
