Zustand: offen — Ausgang noch nicht zugewiesen (unter der 3×-Schwelle). Kein
Träger vorgeschlagen: ob eine mechanische Prüfung (z. B. ein `d-check`-Modul,
das `trace.coverage`-Pfade gegen den Ist-Bestand hält) den Aufwand
rechtfertigt, entscheidet der Lese-Schritt bei 3×; bei 1× nicht fällig.

Zähler (abgeleitet): 1× (evidence/slice-d-check-trace-rtm.md).
