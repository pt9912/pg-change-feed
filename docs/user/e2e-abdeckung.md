# E2E-Abdeckung je Spec-Kennung

Erzeugt von `make test-integration` über `tools/harness/run-integration-tests.sh`:
die Go-Zeilen leitet das Testpaket aus seinem eigenen Quelltext ab
(`TestAbdeckungstabelleZeilen` in `test/integration/integration_test.go`),
die Bash-Zeilen deklariert jede Phase des Runners an Ort und Stelle über
einen Anker. Diese Datei ist eine **stabile Abdeckungs-Deklaration**, kein
Lauf-Beleg: der Runner schreibt sie nur bei inhaltlicher Abweichung. Sie
ändert sich mit den Nachweis-Deklarationen und mit dem Ort ihrer Quellen —
die Spalte `Ort` bindet an `Datei:Zeile`, jede Einfügung oberhalb einer
`func TestE2E*` oder einer Runner-Phase verschiebt die Zeilennummer und
damit diese Tabelle, ohne dass sich an der Abdeckung etwas ändert. Die
Beschreibungsspalte trägt keine Kennungen; die Aussage eines Nachweises steht
über die Spalte `Ort` an ihrer Quelle.

| Spec-Kennung | Kurzbeschreibung | Nachweis | Ort |
| --- | --- | --- | --- |
| [`LH-FA-CAP-001`](../../spec/lastenheft.md), [`LH-FA-CAP-002`](../../spec/lastenheft.md), [`LH-FA-CAP-003`](../../spec/lastenheft.md), [`LH-FA-CAP-004`](../../spec/lastenheft.md), [`LH-QA-POR-003`](../../spec/lastenheft.md), [`LH-FA-SST-001`](../../spec/lastenheft.md), [`LH-FA-CAP-008`](../../spec/lastenheft.md) | trägt den E2E-Ablauf am verdrahteten Feed-Container: INSERT, UPDATE und DELETE an der aktivierten Tabelle werden Ende-zu-Ende durch das Binary erfasst, in Commit-Reihenfolge persistiert und mit Inhalt gelesen. | `TestE2ECaptureFlow` | `test/integration/integration_test.go:197` |
| [`LH-FA-CAP-008`](../../spec/lastenheft.md) | trägt die REPLICA-IDENTITY-FULL-Seite der Row Images am verdrahteten Feed-Container: mit voller Identity trägt die Quelle den kompletten Alt-Stand — `old_data` trägt beide Spalten, nicht nur den Schlüssel. | `TestE2EUpdateOldImageWithFullReplicaIdentity` | `test/integration/integration_test.go:305` |
| [`LH-FA-SST-002`](../../spec/lastenheft.md), [`LH-FA-REA-002`](../../spec/lastenheft.md), [`LH-FA-REA-003`](../../spec/lastenheft.md), [`LH-FA-REA-004`](../../spec/lastenheft.md), [`LH-FA-REA-005`](../../spec/lastenheft.md), [`LH-FA-REA-006`](../../spec/lastenheft.md) | belegt `BEO-PGC/lese-doppelquelle`: derselbe INSERT/UPDATE/DELETE-Rundlauf, gelesen über die externe SQL-Sicht `cdc.changes` statt über den internen Store-Adapter (`ReadChanges`), trägt dieselbe Reihenfolge (`commit_position`, `sequence`) und denselben Feldinhalt (`operation`, `old_data`/`new_data`, `schema_version`) wie die `ReadChanges`-Lesung desselben Datensatzes. | `TestE2EChangesViewMatchesReadChanges` | `test/integration/integration_test.go:428` |
| [`LH-FA-RET-005`](../../spec/lastenheft.md) | belegt: `cdc.retention_blockers` zeigt je Quelle real den Consumer mit der am weitesten zurückliegenden bestätigten Position — der Consumer, dessen Position `RetentionPolicy.AllowsDeletion` (`RunRetentionUseCase`) aktuell als Löschgrenze der Quelle behandelt. | `TestE2ERetentionBlockersViewShowsFurthestBehindConsumer` | `test/integration/integration_test.go:503` |
| [`LH-FA-RET-006`](../../spec/lastenheft.md), [`SPEC-009`](../../spec/pflichtenheft.md) | belegt (`cdc_storage_bytes`) am verdrahteten Feed-Container: `cdc.metrics` trägt nach einer realen CDC-Erfassung einen positiven Wert für die physische Speichergröße von `cdc.change` — derselbe externe SQL-Lesezugriffsweg wie der bestehende `cdc_capture_lag`-Beleg (`tools/harness/run-integration-tests.sh`), hier gegen dieselbe View. | `TestE2EMetricsCarriesStorageBytes` | `test/integration/integration_test.go:619` |
| [`LH-FA-CFG-003`](../../spec/lastenheft.md), [`LH-FA-CFG-004`](../../spec/lastenheft.md) | liest den Aktivierungsstand am verdrahteten Feed-Container über die Status- und Listen-Use-Cases: die aktivierte Feed-Tabelle meldet „aktiviert", die nie aktivierte Tabelle meldet „nicht aktiviert" (Boundary), die fehlende Tabelle endet sichtbar (Negative), und die Liste trägt die aktivierten Tabellen der Quelle. | `TestE2EActivationState` | `test/integration/integration_test.go:647` |
| [`LH-FA-SST-002`](../../spec/lastenheft.md), [`LH-FA-CFG-003`](../../spec/lastenheft.md), [`LH-FA-CFG-004`](../../spec/lastenheft.md) | liest den Aktivierungsstand am verdrahteten Feed-Container über die externe SQL-Sicht `cdc.active_tables` statt über den internen Status-Use-Case: die aktivierte Feed-Tabelle erscheint in der Sicht (Happy Path), die nie aktivierte Tabelle nicht (Boundary) — beide Lesewege tragen dieselbe Aussage über denselben Bindungszustand. | `TestE2EActiveTablesViewMatchesActivationState` | `test/integration/integration_test.go:723` |
| [`LH-FA-CFG-002`](../../spec/lastenheft.md) | trägt die Deaktivierung mit Change-Bestand am verdrahteten Feed-Container (Out-of-Scope): die Erfassung stoppt über den Publication-Entzug, die Bindungs-Zeile bleibt als Herkunft der persistierten Changes — Status und Liste trennen den Herkunfts-Bestand vom Erfassungs-Zustand. | `TestE2EDisableRetainedState` | `test/integration/integration_test.go:788` |
| [`LH-FA-SCH-001`](../../spec/lastenheft.md), [`LH-FA-SCH-002`](../../spec/lastenheft.md), [`LH-FA-SCH-005`](../../spec/lastenheft.md) | trägt eine reale `ALTER TABLE … ADD COLUMN` auf der aktivierten Tabelle `feed_e2e_schema` am verdrahteten Feed-Container (Happy Path, Boundary, Boundary): die danach erfassten Changes tragen die neue Spalte im Row Image, die zuvor erfasste Change bleibt über `cdc.changes` unverändert lesbar und ohne die neue Spalte — und beide Changes tragen unterscheidbare Schema-Versionen. | `TestE2ESchemaChangeAddColumn` | `test/integration/integration_test.go:870` |
| [`LH-FA-SCH-003`](../../spec/lastenheft.md), [`LH-FA-SCH-004`](../../spec/lastenheft.md) | trägt eine reale `ALTER TABLE … DROP COLUMN` auf der aktivierten Tabelle `feed_e2e_schema` am verdrahteten Feed-Container (Happy Path und Boundary, Supersedes Entscheidung 1): eine real entfernte Spalte fehlt in der eingehenden Relation-Nachricht genauso wie jede andere gelöschte Spalte und löst denselben `relationOther`-Pfad mit `mapper.ErrIncompatibleSchemaChange` aus wie eine inkompatible Typänderung (bereits von Teilfrage 4 akzeptierter Präzedenzfall) — sichtbarer `schema`-Fehler, kein stilles Auslassen. | `TestE2ESchemaChangeDropColumn` | `test/integration/integration_test.go:935` |
| [`LH-FA-DAT-006`](../../spec/lastenheft.md) | trägt: eine reale, additive Erweiterung des internen Change-Schemas (`cdc.change`) lässt bereits gespeicherte Changes unverändert über `cdc.changes` lesbar — Schreib- und Lesepfad (`InsertChange`/ `SelectChanges`, `internal/adapters/driven/postgresstorage/queries/queries.go`) tragen explizite Spaltenlisten statt `SELECT *`/positioneller Vollständigkeit, und die deklarative `changes`-View (`tools/schema/schema.yaml`) trägt dieselbe Disziplin mit einer sichtbaren `columns:`-Signatur. | `TestE2EChangeTableMetadataExtensibility` | `test/integration/integration_test.go:996` |
| [`LH-FA-ADM-002`](../../spec/lastenheft.md), [`LH-QA-OPS-002`](../../spec/lastenheft.md) | trägt (Betriebsstatus, Happy Path) am verdrahteten Feed-Container: `cdc.heartbeat` trägt für die laufende Quelle eine frische, fehlerfreie Lebenszeichen-Zeile — derselbe externe SQL-Lesezugriffsweg wie `awaitHeartbeatErrorClass` unten, gegen dieselbe Projektion (`tools/schema/nacharbeit-heartbeat.sql`). | `TestE2EHeartbeatHealthy` | `test/integration/integration_test.go:1068` |
| [`LH-FA-SCH-004`](../../spec/lastenheft.md), [`LH-FA-ADM-003`](../../spec/lastenheft.md) | trägt an der aktivierten Tabelle `feed_e2e_schema` (nach TestE2ESchemaChangeAddColumn, mit der dort hinzugefügten Spalte `extra`) in beiden im Slice-Plan (§6) benannten Fällen: | `TestE2ESchemaChangeIncompatibleTypeChange` | `test/integration/integration_test.go:1157` |
| [`LH-QA-SEC-001`](../../spec/lastenheft.md), [`LH-QA-SEC-002`](../../spec/lastenheft.md), [`LH-QA-SEC-003`](../../spec/lastenheft.md) | die drei Gruppenrollen real gegeneinander geprüft: ein Reader-Login scheitert am schreibenden Aufruf, eine Replication-Verbindung ohne REPLICATION-Attribut scheitert, dieselbe Verbindung mit der Capture-Rolle gelingt | `Rollen-DSN-Verifikation` | `tools/harness/run-integration-tests.sh:314` |
| [`LH-FA-CFG-005`](../../spec/lastenheft.md), [`LH-QA-SEC-004`](../../spec/lastenheft.md) | eine nicht gelistete Tabelle wird über den SQL-Antragsweg aktiviert, der Spaltenausschluss real verarbeitet — die danach erfasste Change trägt den Schlüssel nicht mehr im Row Image, die davor erfasste bleibt mit ihrem Wert unverändert lesbar | `Spaltenausschluss-Rundlauf` | `tools/harness/run-integration-tests.sh:583` |
| [`LH-FA-CFG-005`](../../spec/lastenheft.md), [`LH-QA-SEC-004`](../../spec/lastenheft.md) | nach einem realen Container-Neustart leitet der Prozessstart den dauerhaften Ausschlussstand aus den applied-Zeilen der Spalten-Anträge ab — die danach erfasste Change trägt den Schlüssel nicht mehr, die nicht ausgeschlossene Spalte bleibt | `Spaltenausschluss-Neustart-Beleg` | `tools/harness/run-integration-tests.sh:658` |
| [`LH-FA-CFG-005`](../../spec/lastenheft.md) | der Ausschluss einer an der Quelle fehlenden Spalte endet real failed samt Fehlertext, nicht still applied | `Spaltenausschluss-Negative-Beleg` | `tools/harness/run-integration-tests.sh:704` |
| [`LH-FA-ADM-004`](../../spec/lastenheft.md), [`LH-QA-PER-004`](../../spec/lastenheft.md) | cdc_capture_lag bildet den Abstand zwischen Quelländerung und CDC-Verfügbarkeit ab: eine durch eine pausierte CDC-Runtime künstlich verzögerte Transaktion liegt über der ungehinderten — dieselbe Latenz-Messmethode, die `LH-QA-PER-004` über `LH-FA-ADM-004` verlangt | `Lasttest-Beleg cdc_capture_lag` | `tools/harness/run-integration-tests.sh:789` |
| [`LH-FA-RET-005`](../../spec/lastenheft.md) | cdc.retention_blockers zeigt real den Consumer mit der am weitesten zurückliegenden bestätigten Position als aktuellen Blocker der Quelle — und nach der Bestätigung über die Position hinweg keinen mehr | `Retention-Lebenszyklus-Rundlauf (Blocker-Sichtbarkeit)` | `tools/harness/run-integration-tests.sh:915` |
| [`LH-FA-RET-002`](../../spec/lastenheft.md), [`LH-FA-RET-003`](../../spec/lastenheft.md), [`LH-FA-RET-004`](../../spec/lastenheft.md), [`LH-FA-RET-005`](../../spec/lastenheft.md), [`LH-FA-RET-006`](../../spec/lastenheft.md) | eine isolierte Zeile und ein eigener Consumer durchlaufen real Blocker-Sichtbarkeit, Bestätigung über die Position hinweg, Löschung sowie die durchgehende numerische Lesbarkeit der Speichergröße | `Retention-Lebenszyklus-Rundlauf (kombiniert)` | `tools/harness/run-integration-tests.sh:986` |
| [`LH-FA-RET-005`](../../spec/lastenheft.md), [`LH-FA-RET-006`](../../spec/lastenheft.md) | die diagnose-Ausgabe zeigt vor jeder Consumer-Bestätigung 'kein Blocker' und eine numerische Speichergröße | `Retention-Sichtbarkeits-Beleg (kein Blocker)` | `tools/harness/run-integration-tests.sh:1019` |
| [`LH-QA-POR-003`](../../spec/lastenheft.md) | register-consumer und acknowledge-consumer laufen ausschließlich als externe docker exec-Aufrufe gegen den Produktions-Binary, über einen simulierten Container-Neustart hinweg | `Black-Box-CLI-Rundlauf` | `tools/harness/run-integration-tests.sh:1137` |
| [`LH-FA-ADM-005`](../../spec/lastenheft.md) | cdc.consumer_status zeigt den realen Rückstand über eine reine SQL-Lesung und nach der zweiten Bestätigung null | `Verarbeitungsrückstand-Beleg` | `tools/harness/run-integration-tests.sh:1223` |
| [`LH-FA-SST-003`](../../spec/lastenheft.md), [`LH-FA-ADM-002`](../../spec/lastenheft.md), [`LH-FA-ADM-003`](../../spec/lastenheft.md), [`LH-FA-ADM-004`](../../spec/lastenheft.md), [`LH-FA-ADM-005`](../../spec/lastenheft.md) | der diagnose-Sondermodus trägt im Normalbetrieb alle vier Signale: Betriebsstatus, fehlenden Fehlerzustand, numerischen CDC-Abstand und die Rückstände der geführten Consumer | `CLI-Diagnose-Beleg (Normalbetrieb)` | `tools/harness/run-integration-tests.sh:1279` |
| [`LH-FA-ADM-003`](../../spec/lastenheft.md) | ein direkt in die Heartbeat-Projektion geschriebener Fehlerzustand ist in der diagnose-Ausgabe von Normalbetrieb unterscheidbar, ohne den laufenden Feed-Container zu beenden | `CLI-Diagnose-Beleg (Fehlerzustand)` | `tools/harness/run-integration-tests.sh:1328` |
| [`LH-FA-ADM-001`](../../spec/lastenheft.md), [`LH-FA-CFG-001`](../../spec/lastenheft.md), [`LH-FA-CFG-006`](../../spec/lastenheft.md) | eine bewusst nicht in der Bindungsliste geführte Tabelle wird über den SQL-Antrag aktiviert und vom bereits laufenden Feed-Container ohne Neustart erfasst — ihre eigene DDL (Spalten, Trigger) bleibt dabei real unverändert, keine Anwendungscode-Anpassung nötig | `SQL-Administration Live-Reload (enable)` | `tools/harness/run-integration-tests.sh:1419` |
| [`LH-FA-CFG-002`](../../spec/lastenheft.md) | derselbe Antrags-Weg spiegelbildlich: die Deaktivierung beendet die Erfassung dieser Tabelle, der Prozess läuft weiter | `SQL-Administration Live-Reload (disable)` | `tools/harness/run-integration-tests.sh:1474` |
| [`LH-FA-CFG-002`](../../spec/lastenheft.md) | ein direkter Publication-Entzug per DDL trennt die PostgreSQL-seitige Filterung von der App-seitigen Assembler-Filterung: die Assembler-Bindung bleibt über den ganzen Beleg aktiv | `Publication-Entzug-Wirksamkeit` | `tools/harness/run-integration-tests.sh:1570` |
| [`LH-FA-RET-002`](../../spec/lastenheft.md), [`LH-FA-RET-003`](../../spec/lastenheft.md), [`LH-FA-RET-004`](../../spec/lastenheft.md) | eine zurückdatierte Zeile bleibt erhalten, solange ein Consumer zurückhängt, und wird nach der Freigabe durch beide Consumer real entfernt; die zu junge Zeile bleibt durchgehend erhalten | `Retention-Beleg (Alters- und Consumer-Freigabe)` | `tools/harness/run-integration-tests.sh:1695` |
| [`LH-FA-SST-007`](../../spec/lastenheft.md) | ein Test-Subscriber abonniert das tabellen-granulare Subjekt real vor der Change und empfängt danach real das leere Wecksignal | `NATS-Happy-Path-Beleg` | `tools/harness/run-integration-tests.sh:1774` |
| [`LH-FA-SST-007`](../../spec/lastenheft.md) | eine Change ohne jeden auf dem Subjekt abonnierten Client bleibt vollständig über cdc.changes lesbar, und der Feed-Container läuft unverändert weiter | `NATS-Boundary-Beleg` | `tools/harness/run-integration-tests.sh:1828` |
| [`LH-FA-SST-007`](../../spec/lastenheft.md) | ein real vom Compose-Netz getrennter Subscriber verpasst die Change ohne jedes Wecksignal; ein frischer Wiederverbindungs-Subscriber empfängt die nächste | `NATS-Negative-Beleg (Reconnect-Nachholen)` | `tools/harness/run-integration-tests.sh:1982` |
| [`LH-FA-SST-005`](../../spec/lastenheft.md), [`LH-FA-SST-006`](../../spec/lastenheft.md) | ein Wegwerf-Client ruft RegisterConsumer mit dem admin-Token und ListTables mit dem reader-Token real per HTTP gegen den laufenden Feed-Container auf und liest zusätzlich Changes über `GET /changes` mit dem reader-Token; die Registrierung wird gegen cdc.consumer bestätigt, der gelesene Change gegen cdc.changes — die spätere API, deren Ermöglichung `LH-FA-SST-005` forderte, ohne das interne CDC-Modell zu verändern | `HTTP-API-Rundlauf` | `tools/harness/run-integration-tests.sh:2097` |
| [`LH-FA-CON-006`](../../spec/lastenheft.md) | derselbe Wegwerf-Client bestätigt real per HTTP eine Position für den zuvor registrierten Consumer (macht ihn zum Retention-Blocker der Quelle, real gegen cdc.retention_blockers geprüft) und entfernt ihn danach über POST /consumers/remove mit dem admin-Token; cdc.consumer und cdc.consumer_position tragen ihn danach beide nicht mehr | `HTTP-API-Consumer-Entfernung` | `tools/harness/run-integration-tests.sh:2170` |
| [`LH-QA-OPS-004`](../../spec/lastenheft.md) | ein Wegwerf-Werkzeug liest die stdout-Logzeilen des laufenden Feed-Containers real per docker logs und prüft jede nicht-leere Zeile als eigenständiges JSON-Objekt mit den Feldern time/level/msg | `Strukturiertes-Logging-Beleg` | `tools/harness/run-integration-tests.sh:2200` |
| [`LH-QA-OPS-003`](../../spec/lastenheft.md) | ein eigener, zurückliegender Consumer und ein direkt in die Heartbeat-Projektion geschriebener Fehlerzustand belegen real über cdc.metrics die beiden bislang fehlenden Dimensionen: ausstehende Changes je Consumer und Fehler je Klasse | `Metriken-Minimum-Beleg (ausstehende Changes, Fehlerklassen)` | `tools/harness/run-integration-tests.sh:2253` |
| [`LH-FA-SST-008`](../../spec/lastenheft.md) | ein Wegwerf-Client öffnet real über gRPC den Server-Stream gegen den laufenden Feed-Container und empfängt eine danach committete Änderung; ein Öffnungsversuch ohne gültiges Token endet mit gRPC-Status Unauthenticated | `gRPC-Stream-Rundlauf` | `tools/harness/run-integration-tests.sh:2400` |
| [`LH-FA-SST-008`](../../spec/lastenheft.md) | ein Wegwerf-Client öffnet real per HTTP den Endpunkt `GET /changes/stream` gegen den laufenden Feed-Container und empfängt eine danach committete Änderung; ein Aufruf ohne gültiges Token endet mit HTTP-Status 401 | `SSE-Stream-Rundlauf` | `tools/harness/run-integration-tests.sh:2543` |
| [`LH-FA-SST-008`](../../spec/lastenheft.md) | ein Wegwerf-Client verbindet sich real über NATS mit gültigem Token, abonniert cdc.stream.<...> und empfängt eine danach committete Änderung als vollständiges JSON-Event; ein Verbindungsversuch ohne und einer mit falschem Token werden vom NATS-Server abgelehnt; das bestehende Wecksignal (natssub) funktioniert mit demselben Test-Token unverändert weiter | `NATS-Vollinhalts-Stream-Rundlauf` | `tools/harness/run-integration-tests.sh:2682` |
| [`LH-QA-OPS-005`](../../spec/lastenheft.md) | ein realer Container-Tausch ersetzt den Feed-Container durch eine neue Instanz desselben Images, während Datenbank und NATS unberührt bleiben — der Datenstand davor bleibt lesbar, danach Eingefügtes wird weiter erfasst | `Upgrade-Sicherheits-Rundlauf` | `tools/harness/run-integration-tests.sh:2781` |
| [`LH-FA-SCH-003`](../../spec/lastenheft.md) | nach dem dauerhaften Ende des Erfassungspfads stellt der Runner einen sauberen Zustand wieder her: Replication-Slot neu angelegt, aktuelle Schema-Version nachgetragen, Feed-Container wieder healthy | `Schema-Wiederanlauf nach der Spaltenentfernung` | `tools/harness/run-integration-tests.sh:2875` |
