# E2E-Abdeckung je Spec-Kennung

Erzeugt von `make test-integration` über `tools/harness/run-integration-tests.sh`:
die Go-Zeilen leitet das Testpaket aus seinem eigenen Quelltext ab
(`TestAbdeckungstabelleZeilen` in `test/integration/integration_test.go`),
die Bash-Zeilen deklariert jede Phase des Runners an Ort und Stelle über
einen Anker. Diese Datei ist eine **stabile Abdeckungs-Deklaration**, kein
Lauf-Beleg: sie ändert sich mit den Nachweis-Deklarationen, nicht mit jedem
Lauf — der Runner schreibt sie nur bei inhaltlicher Abweichung. Die
Beschreibungsspalte trägt keine Kennungen; die Aussage eines Nachweises steht
über die Spalte `Ort` an ihrer Quelle.

| Spec-Kennung | Nachweis | Ort | Kurzbeschreibung |
| --- | --- | --- | --- |
| [`LH-FA-CAP-001`](../../spec/lastenheft.md), [`LH-FA-CAP-002`](../../spec/lastenheft.md), [`LH-FA-CAP-003`](../../spec/lastenheft.md), [`LH-FA-CAP-004`](../../spec/lastenheft.md), [`LH-QA-POR-003`](../../spec/lastenheft.md), [`LH-FA-CAP-008`](../../spec/lastenheft.md) | `TestE2ECaptureFlow` | `test/integration/integration_test.go:190` | trägt den E2E-Ablauf am verdrahteten Feed-Container: INSERT, UPDATE und DELETE an der aktivierten Tabelle werden Ende-zu-Ende durch das Binary erfasst, in Commit-Reihenfolge persistiert und mit Inhalt gelesen. |
| [`LH-FA-CAP-008`](../../spec/lastenheft.md) | `TestE2EUpdateOldImageWithFullReplicaIdentity` | `test/integration/integration_test.go:298` | trägt die REPLICA-IDENTITY-FULL-Seite der Row Images am verdrahteten Feed-Container: mit voller Identity trägt die Quelle den kompletten Alt-Stand — `old_data` trägt beide Spalten, nicht nur den Schlüssel. |
| [`LH-FA-SST-002`](../../spec/lastenheft.md), [`LH-FA-REA-002`](../../spec/lastenheft.md), [`LH-FA-REA-003`](../../spec/lastenheft.md), [`LH-FA-REA-004`](../../spec/lastenheft.md), [`LH-FA-REA-005`](../../spec/lastenheft.md), [`LH-FA-REA-006`](../../spec/lastenheft.md) | `TestE2EChangesViewMatchesReadChanges` | `test/integration/integration_test.go:421` | belegt `BEO-PGC/lese-doppelquelle`: derselbe INSERT/UPDATE/DELETE-Rundlauf, gelesen über die externe SQL-Sicht `cdc.changes` statt über den internen Store-Adapter (`ReadChanges`), trägt dieselbe Reihenfolge (`commit_position`, `sequence`) und denselben Feldinhalt (`operation`, `old_data`/`new_data`, `schema_version`) wie die `ReadChanges`-Lesung desselben Datensatzes. |
| [`LH-FA-RET-005`](../../spec/lastenheft.md) | `TestE2ERetentionBlockersViewShowsFurthestBehindConsumer` | `test/integration/integration_test.go:496` | belegt: `cdc.retention_blockers` zeigt je Quelle real den Consumer mit der am weitesten zurückliegenden bestätigten Position — der Consumer, dessen Position `RetentionPolicy.AllowsDeletion` (`RunRetentionUseCase`) aktuell als Löschgrenze der Quelle behandelt. |
| [`LH-FA-RET-006`](../../spec/lastenheft.md), [`SPEC-009`](../../spec/pflichtenheft.md) | `TestE2EMetricsCarriesStorageBytes` | `test/integration/integration_test.go:612` | belegt (`cdc_storage_bytes`) am verdrahteten Feed-Container: `cdc.metrics` trägt nach einer realen CDC-Erfassung einen positiven Wert für die physische Speichergröße von `cdc.change` — derselbe externe SQL-Lesezugriffsweg wie der bestehende `cdc_capture_lag`-Beleg (`tools/harness/run-integration-tests.sh`), hier gegen dieselbe View. |
| [`LH-FA-CFG-003`](../../spec/lastenheft.md), [`LH-FA-CFG-004`](../../spec/lastenheft.md) | `TestE2EActivationState` | `test/integration/integration_test.go:640` | liest den Aktivierungsstand am verdrahteten Feed-Container über die Status- und Listen-Use-Cases: die aktivierte Feed-Tabelle meldet „aktiviert", die nie aktivierte Tabelle meldet „nicht aktiviert" (Boundary), die fehlende Tabelle endet sichtbar (Negative), und die Liste trägt die aktivierten Tabellen der Quelle. |
| [`LH-FA-SST-002`](../../spec/lastenheft.md), [`LH-FA-CFG-003`](../../spec/lastenheft.md), [`LH-FA-CFG-004`](../../spec/lastenheft.md) | `TestE2EActiveTablesViewMatchesActivationState` | `test/integration/integration_test.go:716` | liest den Aktivierungsstand am verdrahteten Feed-Container über die externe SQL-Sicht `cdc.active_tables` statt über den internen Status-Use-Case: die aktivierte Feed-Tabelle erscheint in der Sicht (Happy Path), die nie aktivierte Tabelle nicht (Boundary) — beide Lesewege tragen dieselbe Aussage über denselben Bindungszustand. |
| [`LH-FA-CFG-002`](../../spec/lastenheft.md) | `TestE2EDisableRetainedState` | `test/integration/integration_test.go:781` | trägt die Deaktivierung mit Change-Bestand am verdrahteten Feed-Container (Out-of-Scope): die Erfassung stoppt über den Publication-Entzug, die Bindungs-Zeile bleibt als Herkunft der persistierten Changes — Status und Liste trennen den Herkunfts-Bestand vom Erfassungs-Zustand. |
| [`LH-FA-SCH-001`](../../spec/lastenheft.md), [`LH-FA-SCH-002`](../../spec/lastenheft.md), [`LH-FA-SCH-005`](../../spec/lastenheft.md) | `TestE2ESchemaChangeAddColumn` | `test/integration/integration_test.go:863` | trägt eine reale `ALTER TABLE … ADD COLUMN` auf der aktivierten Tabelle `feed_e2e_schema` am verdrahteten Feed-Container (Happy Path, Boundary, Boundary): die danach erfassten Changes tragen die neue Spalte im Row Image, die zuvor erfasste Change bleibt über `cdc.changes` unverändert lesbar und ohne die neue Spalte — und beide Changes tragen unterscheidbare Schema-Versionen. |
| [`LH-FA-SCH-003`](../../spec/lastenheft.md), [`LH-FA-SCH-004`](../../spec/lastenheft.md) | `TestE2ESchemaChangeDropColumn` | `test/integration/integration_test.go:928` | trägt eine reale `ALTER TABLE … DROP COLUMN` auf der aktivierten Tabelle `feed_e2e_schema` am verdrahteten Feed-Container (Happy Path und Boundary, Supersedes Entscheidung 1): eine real entfernte Spalte fehlt in der eingehenden Relation-Nachricht genauso wie jede andere gelöschte Spalte und löst denselben `relationOther`-Pfad mit `mapper.ErrIncompatibleSchemaChange` aus wie eine inkompatible Typänderung (bereits von Teilfrage 4 akzeptierter Präzedenzfall) — sichtbarer `schema`-Fehler, kein stilles Auslassen. |
| [`LH-FA-DAT-006`](../../spec/lastenheft.md) | `TestE2EChangeTableMetadataExtensibility` | `test/integration/integration_test.go:989` | trägt: eine reale, additive Erweiterung des internen Change-Schemas (`cdc.change`) lässt bereits gespeicherte Changes unverändert über `cdc.changes` lesbar — Schreib- und Lesepfad (`InsertChange`/ `SelectChanges`, `internal/adapters/driven/postgresstorage/queries/queries.go`) tragen explizite Spaltenlisten statt `SELECT *`/positioneller Vollständigkeit, und die deklarative `changes`-View (`tools/schema/schema.yaml`) trägt dieselbe Disziplin mit einer sichtbaren `columns:`-Signatur. |
| [`LH-FA-ADM-002`](../../spec/lastenheft.md), [`LH-QA-OPS-002`](../../spec/lastenheft.md) | `TestE2EHeartbeatHealthy` | `test/integration/integration_test.go:1061` | trägt (Betriebsstatus, Happy Path) am verdrahteten Feed-Container: `cdc.heartbeat` trägt für die laufende Quelle eine frische, fehlerfreie Lebenszeichen-Zeile — derselbe externe SQL-Lesezugriffsweg wie `awaitHeartbeatErrorClass` unten, gegen dieselbe Projektion (`tools/schema/nacharbeit-heartbeat.sql`). |
| [`LH-FA-SCH-004`](../../spec/lastenheft.md), [`LH-FA-ADM-003`](../../spec/lastenheft.md) | `TestE2ESchemaChangeIncompatibleTypeChange` | `test/integration/integration_test.go:1150` | trägt an der aktivierten Tabelle `feed_e2e_schema` (nach TestE2ESchemaChangeAddColumn, mit der dort hinzugefügten Spalte `extra`) in beiden im Slice-Plan (§6) benannten Fällen: |
| [`LH-QA-SEC-001`](../../spec/lastenheft.md), [`LH-QA-SEC-002`](../../spec/lastenheft.md), [`LH-QA-SEC-003`](../../spec/lastenheft.md) | `Rollen-DSN-Verifikation` | `tools/harness/run-integration-tests.sh:295` | die drei Gruppenrollen real gegeneinander geprüft: ein Reader-Login scheitert am schreibenden Aufruf, eine Replication-Verbindung ohne REPLICATION-Attribut scheitert, dieselbe Verbindung mit der Capture-Rolle gelingt |
| [`LH-FA-CFG-005`](../../spec/lastenheft.md), [`LH-QA-SEC-004`](../../spec/lastenheft.md) | `Spaltenausschluss-Rundlauf` | `tools/harness/run-integration-tests.sh:564` | eine nicht gelistete Tabelle wird über den SQL-Antragsweg aktiviert, der Spaltenausschluss real verarbeitet — die danach erfasste Change trägt den Schlüssel nicht mehr im Row Image, die davor erfasste bleibt mit ihrem Wert unverändert lesbar |
| [`LH-FA-CFG-005`](../../spec/lastenheft.md), [`LH-QA-SEC-004`](../../spec/lastenheft.md) | `Spaltenausschluss-Neustart-Beleg` | `tools/harness/run-integration-tests.sh:639` | nach einem realen Container-Neustart leitet der Prozessstart den dauerhaften Ausschlussstand aus den applied-Zeilen der Spalten-Anträge ab — die danach erfasste Change trägt den Schlüssel nicht mehr, die nicht ausgeschlossene Spalte bleibt |
| [`LH-FA-CFG-005`](../../spec/lastenheft.md) | `Spaltenausschluss-Negative-Beleg` | `tools/harness/run-integration-tests.sh:685` | der Ausschluss einer an der Quelle fehlenden Spalte endet real failed samt Fehlertext, nicht still applied |
| [`LH-FA-ADM-004`](../../spec/lastenheft.md) | `Lasttest-Beleg cdc_capture_lag` | `tools/harness/run-integration-tests.sh:770` | cdc_capture_lag bildet den Abstand zwischen Quelländerung und CDC-Verfügbarkeit ab: eine durch eine pausierte CDC-Runtime künstlich verzögerte Transaktion liegt über der ungehinderten |
| [`LH-FA-RET-005`](../../spec/lastenheft.md) | `Retention-Lebenszyklus-Rundlauf (Blocker-Sichtbarkeit)` | `tools/harness/run-integration-tests.sh:896` | cdc.retention_blockers zeigt real den Consumer mit der am weitesten zurückliegenden bestätigten Position als aktuellen Blocker der Quelle — und nach der Bestätigung über die Position hinweg keinen mehr |
| [`LH-FA-RET-002`](../../spec/lastenheft.md), [`LH-FA-RET-003`](../../spec/lastenheft.md), [`LH-FA-RET-004`](../../spec/lastenheft.md), [`LH-FA-RET-005`](../../spec/lastenheft.md), [`LH-FA-RET-006`](../../spec/lastenheft.md) | `Retention-Lebenszyklus-Rundlauf (kombiniert)` | `tools/harness/run-integration-tests.sh:967` | eine isolierte Zeile und ein eigener Consumer durchlaufen real Blocker-Sichtbarkeit, Bestätigung über die Position hinweg, Löschung sowie die durchgehende numerische Lesbarkeit der Speichergröße |
| [`LH-FA-RET-005`](../../spec/lastenheft.md), [`LH-FA-RET-006`](../../spec/lastenheft.md) | `Retention-Sichtbarkeits-Beleg (kein Blocker)` | `tools/harness/run-integration-tests.sh:1000` | die diagnose-Ausgabe zeigt vor jeder Consumer-Bestätigung 'kein Blocker' und eine numerische Speichergröße |
| [`LH-QA-POR-003`](../../spec/lastenheft.md) | `Black-Box-CLI-Rundlauf` | `tools/harness/run-integration-tests.sh:1118` | register-consumer und acknowledge-consumer laufen ausschließlich als externe docker exec-Aufrufe gegen den Produktions-Binary, über einen simulierten Container-Neustart hinweg |
| [`LH-FA-ADM-005`](../../spec/lastenheft.md) | `Verarbeitungsrückstand-Beleg` | `tools/harness/run-integration-tests.sh:1204` | cdc.consumer_status zeigt den realen Rückstand über eine reine SQL-Lesung und nach der zweiten Bestätigung null |
| [`LH-FA-SST-003`](../../spec/lastenheft.md), [`LH-FA-ADM-002`](../../spec/lastenheft.md), [`LH-FA-ADM-003`](../../spec/lastenheft.md), [`LH-FA-ADM-004`](../../spec/lastenheft.md), [`LH-FA-ADM-005`](../../spec/lastenheft.md) | `CLI-Diagnose-Beleg (Normalbetrieb)` | `tools/harness/run-integration-tests.sh:1260` | der diagnose-Sondermodus trägt im Normalbetrieb alle vier Signale: Betriebsstatus, fehlenden Fehlerzustand, numerischen CDC-Abstand und die Rückstände der geführten Consumer |
| [`LH-FA-ADM-003`](../../spec/lastenheft.md) | `CLI-Diagnose-Beleg (Fehlerzustand)` | `tools/harness/run-integration-tests.sh:1309` | ein direkt in die Heartbeat-Projektion geschriebener Fehlerzustand ist in der diagnose-Ausgabe von Normalbetrieb unterscheidbar, ohne den laufenden Feed-Container zu beenden |
| [`LH-FA-ADM-001`](../../spec/lastenheft.md), [`LH-FA-CFG-001`](../../spec/lastenheft.md) | `SQL-Administration Live-Reload (enable)` | `tools/harness/run-integration-tests.sh:1380` | eine bewusst nicht in der Bindungsliste geführte Tabelle wird über den SQL-Antrag aktiviert und vom bereits laufenden Feed-Container ohne Neustart erfasst |
| [`LH-FA-CFG-002`](../../spec/lastenheft.md) | `SQL-Administration Live-Reload (disable)` | `tools/harness/run-integration-tests.sh:1435` | derselbe Antrags-Weg spiegelbildlich: die Deaktivierung beendet die Erfassung dieser Tabelle, der Prozess läuft weiter |
| [`LH-FA-CFG-002`](../../spec/lastenheft.md) | `Publication-Entzug-Wirksamkeit` | `tools/harness/run-integration-tests.sh:1531` | ein direkter Publication-Entzug per DDL trennt die PostgreSQL-seitige Filterung von der App-seitigen Assembler-Filterung: die Assembler-Bindung bleibt über den ganzen Beleg aktiv |
| [`LH-FA-RET-002`](../../spec/lastenheft.md), [`LH-FA-RET-003`](../../spec/lastenheft.md), [`LH-FA-RET-004`](../../spec/lastenheft.md) | `Retention-Beleg (Alters- und Consumer-Freigabe)` | `tools/harness/run-integration-tests.sh:1656` | eine zurückdatierte Zeile bleibt erhalten, solange ein Consumer zurückhängt, und wird nach der Freigabe durch beide Consumer real entfernt; die zu junge Zeile bleibt durchgehend erhalten |
| [`LH-FA-SST-007`](../../spec/lastenheft.md) | `NATS-Happy-Path-Beleg` | `tools/harness/run-integration-tests.sh:1735` | ein Test-Subscriber abonniert das tabellen-granulare Subjekt real vor der Change und empfängt danach real das leere Wecksignal |
| [`LH-FA-SST-007`](../../spec/lastenheft.md) | `NATS-Boundary-Beleg` | `tools/harness/run-integration-tests.sh:1789` | eine Change ohne jeden auf dem Subjekt abonnierten Client bleibt vollständig über cdc.changes lesbar, und der Feed-Container läuft unverändert weiter |
| [`LH-FA-SST-007`](../../spec/lastenheft.md) | `NATS-Negative-Beleg (Reconnect-Nachholen)` | `tools/harness/run-integration-tests.sh:1943` | ein real vom Compose-Netz getrennter Subscriber verpasst die Change ohne jedes Wecksignal; ein frischer Wiederverbindungs-Subscriber empfängt die nächste |
| [`LH-FA-SST-006`](../../spec/lastenheft.md) | `HTTP-API-Rundlauf` | `tools/harness/run-integration-tests.sh:1991` | ein Wegwerf-Client ruft RegisterConsumer mit dem admin-Token und ListTables mit dem reader-Token real per HTTP gegen den laufenden Feed-Container auf, die Registrierung wird gegen cdc.consumer bestätigt |
| [`LH-FA-SST-008`](../../spec/lastenheft.md) | `gRPC-Stream-Rundlauf` | `tools/harness/run-integration-tests.sh:2138` | ein Wegwerf-Client öffnet real über gRPC den Server-Stream gegen den laufenden Feed-Container und empfängt eine danach committete Änderung; ein Öffnungsversuch ohne gültiges Token endet mit gRPC-Status Unauthenticated |
| [`LH-FA-SST-008`](../../spec/lastenheft.md) | `SSE-Stream-Rundlauf` | `tools/harness/run-integration-tests.sh:2281` | ein Wegwerf-Client öffnet real per HTTP den Endpunkt `GET /changes/stream` gegen den laufenden Feed-Container und empfängt eine danach committete Änderung; ein Aufruf ohne gültiges Token endet mit HTTP-Status 401 |
| [`LH-QA-OPS-005`](../../spec/lastenheft.md) | `Upgrade-Sicherheits-Rundlauf` | `tools/harness/run-integration-tests.sh:2380` | ein realer Container-Tausch ersetzt den Feed-Container durch eine neue Instanz desselben Images, während Datenbank und NATS unberührt bleiben — der Datenstand davor bleibt lesbar, danach Eingefügtes wird weiter erfasst |
| [`LH-FA-SCH-003`](../../spec/lastenheft.md) | `Schema-Wiederanlauf nach der Spaltenentfernung` | `tools/harness/run-integration-tests.sh:2474` | nach dem dauerhaften Ende des Erfassungspfads stellt der Runner einen sauberen Zustand wieder her: Replication-Slot neu angelegt, aktuelle Schema-Version nachgetragen, Feed-Container wieder healthy |
